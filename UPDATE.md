# 更新日志（本 Fork）

基线版本：上游同步 **3.1.73**（`7001cd2`）。  
下列为相对该基线后的主要增量；更细提交见 git log。

## 相对上游：我们多做了什么

| 方向 | 说明 |
| --- | --- |
| OpenList/AList 离线可靠性 | 硬超时清理、分级轮询、本集完成判定、10008 冷却、残留 adopt、启动回扫 |
| 可观测与干预 | 首页「任务管理器」：多槽位状态、取消、排队/抢先、残留扫描清理 |
| 调度一致性 | 手动刷新 / 添加订阅统一走可观察入口，避免 UI 空闲却只剩 Hash |
| 通知与体验 | Bark 通知（含 Level/Volume）；默认禁用自动检查更新 |

---

## 3.1.73-fork 后续（2026-07）

### 新增
- **RSS 任务管理器**（首页入口）
  - 总览：运行中/空闲、消息、已处理时间
  - 槽位：RSS 运行中、待执行手动刷新、OpenList 当前占用、离线残留、上一轮已处理
  - 操作：刷新、扫描残留、清理残留、单条取消、全部取消
- **手动刷新抢先 / 排队**：周期任务可被手动刷新抢先；忙时最多保留 1 个 pending
- **OpenList 残留扫描 / 清理 API** 与 UI 接线；启动回扫终态残留
- 添加订阅后纳入统一手动刷新，任务管理器可正确显示进度

### OpenList 离线
- **硬超时**：超过用户「离线超时」强制失败，并清理 OpenList 与 ani 侧坏任务，避免卡住正常流程
- **分级轮询**：提交成功后拉长探测间隔，降低频繁访问 API / 重复提交
- **完成判定**：按本集目标文件判断成功，避免同季其它集已存在导致误成功
- **10008**：识别任务内错误并冷却，防重复提交
- **残留 adopt / fs 校验**：复用未完成任务，缩短全局锁，校验文件系统状态
- **取消策略**：OpenList/AList 清理远端离线占用；qB/Aria2/Transmission 仅停 RSS 推进

### 任务管理器体验
- 多卡片列表；展示已处理时间、耗时、Hash（有则显示）
- **无离线残留时不展示**「OpenList 离线残留」空卡片；仅有残留、清理中或扫描异常时显示
- 修复 App 图标/组件名冲突导致入口异常

### 其它
- 选择性合入上游有用改动（下载/种子相关）
- 局部借鉴上游 syncLock/syncDownload，统一刷新入口
- `PART_REG` 支持带边界的裸「上/下」分卷匹配
- Bark 通知；支持 Level、Volume
- Fork：默认禁用自动检查更新

### 使用注意
- 任务管理器是**全局单 RSS 调度**的观察面板，不是并行历史队列
- 重启丢失内存调度态；OpenList 残留靠回扫/扫描恢复
- 升级后请 **完整重启**，否则可能仍跑旧前端
- 反馈请附：下载工具类型、离线超时配置、任务管理器截图与关键日志

---

## 历史摘录（更早）

- refactor: 局部借鉴上游 syncLock/syncDownload，统一手动刷新入口
- feat: Bark 通知支持设置 Level、Volume
- feat: 增加 Bark 通知
- chore: 同步上游版本至 3.1.73
