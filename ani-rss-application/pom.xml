<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ani.rss</groupId>
        <artifactId>ani-rss</artifactId>
        <version>3.4.20</version>
    </parent>

    <artifactId>ani-rss-application</artifactId>

    <properties>
        <hutool.version>5.8.47</hutool.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <repository>
            <id>ebml-reader</id>
            <url>https://raw.github.com/wushuo894/EBMLReader/mvn-repo</url>
        </repository>
        <repository>
            <id>tmdb-api</id>
            <url>https://raw.github.com/wushuo894/tmdb-api/mvn-repo</url>
        </repository>
        <!--
            jlibtorrent（磁力链接元数据解析）只发在 FrostWire 自己的 Maven 仓库，不在 Central。

            必须显式声明 checksumPolicy=fail：该站点对"不存在的 artifact"返回 302 → HTML 200（不是 404），
            默认的 warn 策略会把这段 HTML 当成 POM 收下并写进本地仓库，之后：
              [WARNING] The POM for <artifact> is invalid, transitive dependencies (if any) will not be available
            ——静默丢掉该依赖的<b>全部传递依赖</b>。v3.4.14 首次发版就踩了这个坑：springdoc 的
            common/webmvc-api/swagger-ui/webjars 与 hibernate-validator 全被丢掉，
            运行时 `FileNotFoundException: .../SwaggerUiConfigProperties.class` 直接启动失败。
            fail 策略下这种"假 200"会被判校验失败并丢弃，Maven 回落到 Central 取真件。
        -->
        <repository>
            <id>frostwire-maven</id>
            <url>https://dl.frostwire.com/maven</url>
            <releases>
                <checksumPolicy>fail</checksumPolicy>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.mnode.ical4j</groupId>
            <artifactId>ical4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark</artifactId>
        </dependency>
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse</groupId>
            <artifactId>bittorrent</artifactId>
        </dependency>
        <!--
            磁力链接元数据解析（合集下载支持 magnet）。

            为什么需要原生库：BEP-9 的元数据交换要先接入 DHT/Tracker、与 Peer 握手，
            纯 Java 自研不现实。jlibtorrent 是 libtorrent 的 JNI 绑定，
            上游 ani-rss 亦采用同一方案（wushuo894/ani-rss 6b7dde89）。

            平台包按需引入：缺失的平台（如 32 位 arm、musl）在运行时会抛
            UnsatisfiedLinkError，由 MagnetTorrentUtil 统一转成用户可读的提示，
            不影响其余功能（种子文件走原有 Base64 链路）。
        -->
        <dependency>
            <groupId>com.frostwire</groupId>
            <artifactId>jlibtorrent</artifactId>
            <version>${jlibtorrent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.frostwire</groupId>
            <artifactId>jlibtorrent-windows</artifactId>
            <version>${jlibtorrent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.frostwire</groupId>
            <artifactId>jlibtorrent-linux-x86_64</artifactId>
            <version>${jlibtorrent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.frostwire</groupId>
            <artifactId>jlibtorrent-linux-arm64</artifactId>
            <version>${jlibtorrent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.frostwire</groupId>
            <artifactId>jlibtorrent-macosx-arm64</artifactId>
            <version>${jlibtorrent.version}</version>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>jakarta.mail</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.biezhi</groupId>
            <artifactId>TinyPinyin</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
        </dependency>
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
        </dependency>
        <dependency>
            <groupId>ebml.reader</groupId>
            <artifactId>ebml-reader</artifactId>
        </dependency>
        <dependency>
            <groupId>wushuo.tmdb.api</groupId>
            <artifactId>tmdb-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ani.rss</groupId>
            <artifactId>ani-rss-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Swagger -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-core-jakarta</artifactId>
        </dependency>

        <!-- Spring AI -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>

        <!-- SpringBoot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- hutool -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-cache</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-system</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-json</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-db</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-extra</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-crypto</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-http</artifactId>
            <version>${hutool.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>ani-rss</finalName>
        <!--
          构建卫生: 本机 Windows 组件服务偶尔会以未展开的 "%SystemDrive%" 作为目录名,
          在 src/main/resources 下写出 ProgramData/... 的 COM+ 缓存 .db 文件(约 976KB)。
          若不排除, 它会被 resources 插件复制进 target/classes 并最终打进 jar。
        -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>%SystemDrive%/**</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>ani.rss.AniRssApplication</mainClass>
                </configuration>
            </plugin>
            <!--
              测试分两段执行：
              1) 默认执行 = 普通单测，单个 JVM 复用（快），用 excludedGroups 把端到端测试挡掉；
              2) e2e-tests = 端到端测试（@Tag("e2e")），单独 JVM 且不复用 fork。

              为什么要分开：端到端测试会把整个应用跑起来（Spring 上下文、真实 Tomcat、
              后台任务线程），并设置 -DCONFIG 指向临时目录、把 HTTP 客户端指向假公网。
              这些静态状态与单测共享 JVM 时会互相污染（谁先跑谁说了算，结果不确定）。
              分开后：单测保持快，端到端测试每次都在干净 JVM 里跑，结论可复现。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>e2e</excludedGroups>
                </configuration>
                <executions>
                    <execution>
                        <id>e2e-tests</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <groups>e2e</groups>
                            <excludedGroups combine.self="override"/>
                            <reuseForks>false</reuseForks>
                            <forkCount>1</forkCount>
                            <!-- 端到端测试结束后工件留在 target/e2e-artifact -->
                            <systemPropertyVariables>
                                <file.encoding>UTF-8</file.encoding>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-resources</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>bash</executable>
                            <arguments>
                                <argument>./generate-resources.sh</argument>
                            </arguments>
                            <workingDirectory>${project.basedir}</workingDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>macos</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>package-macos</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <mainClass>ani.rss.build.MacOS</mainClass>
                                    <systemProperties>
                                        <systemProperty>
                                            <key>basedir</key>
                                            <value>${project.basedir}</value>
                                        </systemProperty>
                                    </systemProperties>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>windows</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.akathist.maven.plugins.launch4j</groupId>
                        <artifactId>launch4j-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>l4j</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>launch4j</goal>
                                </goals>
                                <configuration>
                                    <headerType>gui</headerType>
                                    <outfile>target/ani-rss.exe</outfile>
                                    <jar>target/ani-rss.jar</jar>
                                    <errTitle>Java environment is required!</errTitle>
                                    <cmdLine>--gui</cmdLine>
                                    <chdir>.</chdir>
                                    <priority>normal</priority>
                                    <downloadUrl>https://adoptium.net/zh-CN/</downloadUrl>
                                    <supportUrl>https://whichjdk.com/</supportUrl>
                                    <stayAlive>false</stayAlive>
                                    <restartOnCrash>false</restartOnCrash>
                                    <icon>${project.parent.basedir}/ani-rss-ui/public/favicon.ico</icon>
                                    <singleInstance>
                                        <mutexName>ani-rss (${project.version})</mutexName>
                                        <windowTitle>ani-rss (${project.version})</windowTitle>
                                    </singleInstance>
                                    <jre>
                                        <path>%JAVA_HOME%/bin;%PATH%</path>
                                        <minVersion>${java.version}</minVersion>
                                        <opts>
                                            <opt>-Xms64m -Xmx512m -Xss512k -XX:+UseG1GC</opt>
                                            <opt>-XX:+UseStringDeduplication</opt>
                                            <!-- 不要加 -XX:TieredStopAtLevel=1。那是"加快启动"的取舍，
                                                 代价是热点方法永远停留在 C1、C2 永不启用；本项目是常驻服务，
                                                 热点在 JSON 序列化 / 正则 / 字符串处理上，C1-only 会让吞吐下降数倍。
                                                 启动时间请用 AppCDS(-XX:SharedArchiveFile) 优化，不要砍 C2。 -->
                                            <opt>-XX:+HeapDumpOnOutOfMemoryError</opt>
                                            <opt>-XX:+ExitOnOutOfMemoryError</opt>
                                            <opt>-XX:MaxMetaspaceSize=256m</opt>
                                            <!-- IgnoreUnrecognizedVMOptions 必须位于所有非 17 专属选项之前，
                                                 否则 UseCompactObjectHeaders(JDK24+) 在 JDK 17~23 上会导致启动失败 -->
                                            <opt>-XX:+IgnoreUnrecognizedVMOptions</opt>
                                            <opt>-XX:+UseCompactObjectHeaders</opt>
                                            <opt>--enable-native-access=ALL-UNNAMED</opt>
                                            <opt>--add-opens=java.base/java.net=ALL-UNNAMED</opt>
                                            <opt>--add-opens=java.base/sun.net.www.protocol.https=ALL-UNNAMED</opt>
                                            <opt>-Dfile.encoding=UTF-8</opt>
                                        </opts>
                                    </jre>
                                    <versionInfo>
                                        <fileDescription>https://docs.wushuo.top/</fileDescription>
                                        <copyright>Copyright (C) 2024-2025</copyright>
                                        <productName>${project.artifactId}</productName>
                                        <internalName>${project.artifactId}</internalName>
                                        <originalFilename>ani-rss.exe</originalFilename>
                                        <language>SIMPLIFIED_CHINESE</language>
                                    </versionInfo>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
