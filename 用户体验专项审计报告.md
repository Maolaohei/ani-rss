# ani-rss 用户体验（UX）改进专项报告

> 审计方式：按**用户旅程**分 5 片并行深审（订阅卡片与批量管理 / 设置与配置 / 任务与日志可观测性 / 添加订阅主流程 / 播放·登录·移动端·可访问性）+ 父审计员逐条回源复核关键断言。
> 每条均有代码证据（`文件:行号` + 代码片段），已剔除纯代码风格问题、无证据猜测，以及经复核**不成立**的猜测（见 §7）。
> 与既有《稳定性专项审计报告》互补：本报告只谈**用户能不能看懂、能不能找到、操作会不会丢**，不谈崩溃与数据一致性。

---

## 0. 结论速览

产品功能已经相当完整（RSS 解析可靠度、OpenList 离线链路、任务管理器、失败队列都比同类项目深），**当前体验短板集中在三类**：

| 类型 | 一句话 | 典型表现 |
| --- | --- | --- |
| **反馈缺失 / 误导** | 接口失败与"没有数据"在界面上长得一模一样 | 22 处 `v-loading` 只有 4 处出现 `el-empty`；下载器连不上被写成"当前无下载任务" |
| **状态算好了却没透出** | 后端算了、存了，用户看不到 | 漏集数、健康原因、日志时间戳、下载速度/ETA、feature 开关 |
| **保存语义混乱** | "确定/取消"在不同弹窗里含义不同 | 通知弹窗的"确定"不保存；设置弹窗按 Esc 丢全部改动；外观设置点取消却已生效 |

**P0（6 条）**

| # | 问题 | 关键证据 |
| --- | --- | --- |
| P0-1 | 接口失败**无提示、无重试**，界面直接空白；空结果显示为纯白页 | `home/List.vue:100,108-123`、`js/api.js:28-33` |
| P0-2 | 日志面板**没有时间戳、不能搜索**，自部署用户无法自查"为什么没下载" | `entity/Log.java:15-40`、`home/Logs.vue:72-86` |
| P0-3 | 故障时给出**反向结论**：下载器连不上渲染成"当前无下载任务"；任务接口失败静默成"空闲" | `home/TorrentsInfos.vue:23,112-122`、`home/TaskManager.vue:552-560` |
| P0-4 | 通知配置弹窗的「**确定**」不保存，关掉设置弹窗即静默丢失 | `config/NotificationConfig.vue:58-63` + `home/Config.vue:87-108` |
| P0-5 | 设置弹窗**按 Esc / 点遮罩静默丢弃跨页签全部改动**，无脏数据保护 | `home/Config.vue:2, 42-46` |
| P0-6 | 登录被限流时错误提示**在 1 秒后被强制刷新抹掉**，用户只看到表单闪一下、不知道错在哪 | `Login.vue:82-97` + `js/api.js:46-51` |

**P1 共 63 条**、**P2 共 53 条**，集中在：失败归因链断裂、移动端可访问性（触控尺寸/无 aria-label/禁止缩放）、"确定"语义不一致导致的静默丢数据、卡片信息层级、"已订阅"提示不足、设置项无即时校验与首次引导、播放入口三处断层。

**推荐落地顺序**：先做下方「Top 14 快赢」——多数是 1~30 行改动，却能直接消掉用户最多的两类抱怨："我明明操作了却没反应"和"我明明配了却没生效"。

---

## 1. Top 14 快赢（建议一个迭代内做完）

| 排序 | 动作 | 改动量 | 关联 |
| --- | --- | --- | --- |
| 1 | 登录页不复用 `api.js` 的 403 自动 reload；错误改**常驻 `el-alert`**（区分密码错/IP 白名单/限流 + 剩余等待时间）——**这是登录路径当前的唯一出路** | ~20 行 | P0-6 |
| 2 | `js/api.js` 统一兜底网络/JSON 异常 + `main.js` 注册 `app.config.errorHandler` 与 `unhandledrejection` | ~20 行 | P0-1 |
| 3 | `TorrentsInfos.vue` 区分"空 / 查询失败 / 数据过期"三态；`TaskManager.fetchStatus` 失败时置常驻告警并停止重复 toast | ~40 行 | P0-3 |
| 4 | 设置弹窗加 `:before-close` 脏数据确认 + `:close-on-click-modal="false"` | ~15 行 | P0-5 |
| 5 | 通知弹窗「确定」改为显式提交（或改文案为「关闭」+ 父窗未保存标记） | ~10 行 | P0-4 |
| 6 | `BgmRate.vue:53-56` 的 `clearRate` 改调 `setRate(score=0)`——**当前"清空评分"根本清不掉** | 1 行 | P1-45 |
| 7 | `AniBT.vue:328` 的 `list()` → `search()`（不存在的函数导致成功后反报错） | 1 行 | P1-18 |
| 8 | 移动端顶栏按钮补文字标签或 `aria-label`/`title`（含"管理""日志"等纯图标项） | ~10 行 | P1-60 |
| 9 | 定时重算"最近更新"相对时间（30~60s `useIntervalFn`）+ 给该行补文字标签 | ~15 行 | P1-5 |
| 10 | 健康分改 `el-popover`（click 触发）并直出首条原因；卡片渲染 `releaseDate`（周几） | ~25 行 | P1-4、P2-24 |
| 11 | `Manage.vue` 表格上方常驻"已选 N 项 / 清空选择"，批量项在未选中时禁用；底部按钮改"刷新状态" | ~20 行 | P1-9、P1-10 |
| 12 | 修掉残留英文与前后端矛盾文案（"Avatar picture…"、"自动重启 vs 手动重启"） | ~10 行 | P1-13、P1-23 |
| 13 | 6 处 `execCommand('copy')` 改 `navigator.clipboard` + 失败提示；`ElMessage.error(e)` → `e?.message` | ~25 行 | P2-28、P2-27 |
| 14 | 全局 `img` 加 `loading="lazy"` + `onerror` 占位；`AniCard` 的 `score` 空值防护 | ~20 行 | P1-61、P1-8 |

---

## 2. P0：高频路径受阻或信息误导

### P0-1. 接口失败 = 界面空白：全站没有失败态，也没有重试入口
**位置**：`ani-rss-ui/src/home/List.vue:100,108-123`、`ani-rss-ui/src/js/api.js:20-56`、`ani-rss-ui/src/main.js:7-11`

```js
// List.vue —— 只有 then/finally，没有 catch
const getList = () => {
  loading.value = true
  listAni().then(res => { ... }).finally(() => { loading.value = false })
}
```
```js
// api.js —— fetch 与 res.json() 的异常直接 reject，且不弹任何提示
return await fetch(url, {...})
    .then(res => res.json())          // 反代返回 HTML/502 → SyntaxError
    .then(res => { ... ElMessage.error(message) ... })
```
- **触发**：后端容器重启中、反代返回 502/登录页 HTML、网络抖动。此时 `fetch_` 在进入 `code` 判断**之前**就 reject，`ElMessage` 一次都不会触发；`List.vue` 又没有 `catch`，于是首页加载动画结束后**全空**，与"没有订阅"完全无法区分。
- **叠加放大**：筛选条件是持久化的（`App.vue:173` `useLocalStorage('select-enable','已启用')`），用户上次切到"已启用/某个月"后下次打开若零结果，看到的是**纯白页且连"没有结果"都没有**——很容易判断为"订阅被删了/程序坏了"。`List.vue:100` 的 `.filter(it => it.items.length)` 更会把空分组整组丢掉，连组标题都不剩。
- **证据**：全项目 `v-loading` 共 22 处，仅 `About.vue:92`、`TorrentsInfos.vue:23`、`SwitchMaster.vue:41`、`TaskManager.vue:77` 使用 `el-empty`；`List.vue` 没有任何空状态，也没有"筛选后 N / 共 M 项"计数。
- **佐证**：`main.js` 未注册 `app.config.errorHandler`，也没有 `window.onunhandledrejection`，异常只进浏览器控制台。
- **修复**：①`api.js` 统一 catch 网络/解析异常并 `ElMessage.error('网络请求失败，请检查服务是否在运行')`；②`main.js` 注册全局兜底；③首页与各弹窗补"加载失败 + 重试"；④`List.vue` 零结果时渲染 `el-empty` + "清除筛选条件"按钮 + 计数。

### P0-2. 日志面板缺时间戳、缺搜索、不流式：自部署用户无法自查
**位置**：`ani-rss-application/src/main/java/ani/rss/entity/Log.java:15-40`、`ani-rss-ui/src/home/Logs.vue:4-22, 72-86, 111-129`

```java
public class Log implements Serializable {
    private String message;
    private String level;
    private String loggerName;
    private String threadName;      // ← 没有时间戳字段
}
```
```js
// Logs.vue —— 只把 message 拼起来做高亮；过滤维度只有"级别 + Java 类名"
let code = log.map(it => it['message']).join('\r\n');
htmlLogs.value = highlighter.codeToHtml(code, {lang: 'log', theme: 'nord'})
setTimeout(() => { scrollbarRef.value?.setScrollTop(innerRef.value.clientHeight) })
```
- **影响**：用户问"我的番 20 点为什么没下载"，打开日志只有一屏**没有时间**的正文，无法对齐时间线，也无法搜索番剧名——因为过滤维度只有"级别 + Java 类名（`org.ani.rss.xxx`）"。诊断链在最关键的一环断开。
- **补充证据**：无 `visibilitychange`、无轮询，`getLogs()` 只在打开面板与手点刷新时调用（`Logs.vue:26`）；也没有"自动滚动/暂停跟随"开关，任何过滤动作或刷新都强制 `setScrollTop(clientHeight)` 弹到底部，正在往上翻的用户每次被打断。
- **另一个盲区**：`/logs` 返回的 `LOG_LIST` 只由**当前进程**的 appender 追加（`LogUtil.java:38,100-102`），从不从磁盘日志回填——重启（更新/改配置/崩溃重启）后面板是**空的**，而 `config/logs/*.log` 里其实有完整记录。面板对此没有任何说明。
- **修复**：①`Log` 加 `timestamp`（`LogUtil` 写入时已有时间信息）并前端本地化显示；②加关键词过滤 + 时间范围；③加"自动滚动"开关；④面板顶部一行状态说明（进程启动时间 / 缓存上限 / 已被截断 / "下载完整日志"入口），为空时提示"当前进程暂无日志，可下载历史日志 zip"。

### P0-3. 故障时给出反向结论：把"连不上"渲染成"没有"
**位置**：`ani-rss-ui/src/home/TorrentsInfos.vue:23,112-122`、`ani-rss-ui/src/home/TaskManager.vue:552-560,562-580`

```js
// TorrentsInfos.vue —— 异常被静默吞掉，空数组照常渲染
let getTorrentsInfos = async () => {
  while (dialogVisible.value) {
    try { let res = await http.torrentsInfos(); torrentsInfos.value = sortInfos(await res.data) }
    catch (_) { }
```
```html
<el-empty v-if="!torrentsInfos.length" description="当前无下载任务" class="torrents-empty"/>
```
- **影响**：qBittorrent/Transmission/OpenList 认证失败、地址填错、容器未启动时，界面**明确写着"当前无下载任务"**。用户据此判断"没有任务"，而真实情况是下载列表根本查不到——这正是"番没下载下来"最常见的根因之一，却被界面反向误导。后端 `TorrentUtil.getTorrentsInfos` 已刻意不吞异常并上抛，前端却把异常吃掉了。
- **同源问题（任务管理器）**：`fetchStatus` 用 `catch (_) {}` 吞掉异常，面板停在**旧快照**上继续显示"空闲/运行中"；而当接口持续返回错误体时，`api.js:45` 每次都弹 `ElMessage.error`，`pollStatus` 的 `while` 又不中断 → **每 2~4 秒重复弹同一条错误 toast**，直到用户关闭弹窗。同一份代码既"该报不报"又"报得太吵"。
- **修复**：①下载列表区分三态：空 / 查询失败（原因 + "去设置检查下载器"）/ 数据过期（保留上次数据并标注）；②`fetchStatus` 失败时置 `statusError`，顶部常驻 `el-alert`（含"最后成功刷新时间 + 重试"），连续失败后退避或暂停轮询；③对重复错误做去重，只提示一次。

### P0-4. 通知配置弹窗的「确定」不保存
**位置**：`ani-rss-ui/src/config/NotificationConfig.vue:58-63`、`ani-rss-ui/src/home/Config.vue:87-108`

```html
<!-- 通知编辑弹窗底部 -->
<el-button bg text @click="messageTest" icon="Odometer" :loading="messageTestLoading">测试</el-button>
<el-button @click="dialogVisible = false" text bg icon="Check" type="primary">确定</el-button>
```
- **影响**：用户配完 Telegram Token / Bark Key / Emby 地址，点「确定」→ 只关闭了子弹窗；随后习惯性关掉设置弹窗（或按 Esc，见 P0-5）→ 通知配置**全部丢失**。更糟的是「测试」按钮用的是内存对象，会立刻提示成功（`NotificationConfig.vue:122-135`），进一步强化"已保存"的错觉。同一屏里两个「确定」语义不同。
- **修复**：①子弹窗「确定」调用父级提交，或至少把文案改成「关闭」；②父弹窗对未保存的通知配置给出明显标记；③测试按钮旁注明"测试使用当前未保存的设置"。

### P0-5. 设置弹窗按 Esc / 点遮罩静默丢弃全部改动
**位置**：`ani-rss-ui/src/home/Config.vue:2, 42-46`
```html
<el-dialog v-model="dialogVisible" center title="设置">
...
<div class="action">
  <el-button :loading="configButtonLoading" ... @click="saveConfig">确定</el-button>
  <el-button icon="Close" bg text @click="dialogVisible = false">取消</el-button>
</div>
```
- **证据**：`el-dialog` 未传 `close-on-click-modal` / `close-on-press-escape`，Element Plus 默认均为 `true`（已核对 `node_modules/element-plus/es/components/dialog/src/dialog.mjs:137-138`）。8 个页签、上百个字段共用底部唯一一个「确定」，既无 dirty 追踪也无 `before-close`。
- **影响**：用户改了 RSS 间隔、下载器地址、重命名模板后，一次误触遮罩 = 全部白改，且没有任何提示；「取消」与关窗行为也不可区分。
- **修复**：`before-close` 比对快照做脏数据确认；`close-on-click-modal=false`；可选每页签独立保存 + 页签脏标记。

### P0-6. 登录被限流时，错误提示在 1 秒后被强制刷新抹掉
**位置**：`ani-rss-ui/src/Login.vue:82-97`、`ani-rss-ui/src/js/api.js:41-51`、`ani-rss-application/.../util/other/AuthUtil.java:197-203, 255-264`
```js
// api.js —— 任何 403 都清 token 并 1 秒后强制 reload
if (code === 403) {
    authorization.value = ''
    setTimeout(() => { location.reload() }, 1000)
}
```
```js
// Login.vue —— 没有 catch，也没有常驻错误区，只能靠 api.js 的 toast
http.login(user.value).then(res => { ... }).finally(() => { loading.value = false })
```
```java
private static final int LOGIN_ATTEMPT_LIMIT = 30;
private static final long LOGIN_ATTEMPT_WINDOW_MS = TimeUnit.DAYS.toMillis(1);
...
log.warn("登录失败次数达到 {} 次, 已限制登录 1 天: {}", LOGIN_ATTEMPT_LIMIT, ip);
```
- **影响**：密码输错 30 次后，后端进入**整整 1 天**的限流，返回 403；`api.js` 于是清 token 并 1 秒后 `location.reload()`。Element Plus 消息默认展示 3000ms，**提示只显示了 1/3 就被页面刷新销毁**——用户看到的是"点登录 → 表单闪一下 → 又回到登录页"，既不知道是密码错、IP 不在白名单、还是被限流，也不知道要等一天。而这里恰恰是最需要说清楚原因的地方。
- **修复**：①登录页不复用 `api.js` 的 403 自动 reload（或把 403 处理改为"仅当已登录态"才跳转）；②登录失败用**常驻 `el-alert` 错误条**（含"原因 + 剩余等待时间 + 怎么办"），而不是瞬态 toast；③文案区分密码错误 / IP 白名单 / 限流。
- **相关同源问题**：`Login.vue:10-11` 的 `@keyup.enter="login"` 没有并发守卫（`el-button` 的 `loading` 只挡点击不挡回车），用户按住回车会**加速触发这 30 次限流**（见 P1-59）。

---

## 3. P1：明显体验损伤

### 3.1 首页与订阅列表

#### P1-1. 按星期分组时"今天"没有任何标识或置顶
**位置**：`ani-rss-ui/src/home/List.vue:10-13, 168-170`
```html
<div v-for="weekItem in filterList">
  <h2 class="list-week-title">{{ weekItem.weekLabel }}</h2>
```
```css
.list-week-title { margin: 16px 0 8px 4px; }   /* 与其它分组完全同款 */
```
- 全项目检索不到"今天/今日"字样（仅 `weekLabel` 直出）。顺序由后端 `WeekComparator` 决定（确实把今天排在第一位），但界面上没有任何提示，用户每次要先数今天是周几再往下找。追番用户最高频的动作恰恰是"今天更新了哪几部"。
- **修复**：今天分组置顶并加 `el-tag`「今天」+ sticky；可提供"滚动到今天"。

#### P1-2. 搜索无防抖、每键全量深拷贝、大小写敏感、不搜字幕组
**位置**：`ani-rss-ui/src/home/List.vue:72-84`、`ani-rss-ui/src/home/App.vue:13-17`
```js
const changeFilterList = (text = '') => {
  let tempList = weekList.value;
  tempList = JSON.parse(JSON.stringify(tempList))      // 每次按键整表深拷贝
  ...
    return title.indexOf(text) > -1 || pinyin.indexOf(text) > -1 || pinyinInitials.indexOf(text) > -1;
```
```html
<el-input v-model="title" @input="listRef.changeFilterList(title)" ... />   <!-- 无防抖 -->
```
- 订阅上百条时每个字符都要深拷贝整棵树（含 `standbyRssList`）并重建网格，输入明显卡顿；搜 `one piece` 匹配不到 `One Piece`，粘贴尾部带空格就 0 结果，按字幕组搜不到。
- **修复**：`useDebounceFn`（`global.js:166` 的 resize 已在用）包 200ms；匹配前 `trim().toLowerCase()`；把 `subgroup` 纳入匹配字段。

#### P1-3. 首页没有排序入口，也没有分页/虚拟滚动
**位置**：`ani-rss-ui/src/home/List.vue:105`、`js/http.js:22`、`home/App.vue:23-47`
- 排序完全由后端写死的 `item.sort` 决定（受「设置-基本设置-页面-排序」全局配置影响），首页无法临时"按最近更新排一下"；订阅量大时全量挂载所有卡片与 tooltip，滚动/搜索变卡。
- **修复**：首页头部加排序下拉（更新时间/评分/拼音）；`listAni` 支持 `sortType` 参数；配 `content-visibility: auto` 或虚拟滚动。

#### P1-4. 卡片回答不了"下一集什么时候来 / 这集下没下 / 漏没漏"，关键状态被 7 个低价值 tag 淹没
**位置**：`ani-rss-ui/src/home/AniCard.vue:43-79`
```html
<div class="list-card-tags" :class="isNotMobile ? 'gtc3' : 'gtc2'">
  <el-tag>第 {{ item.season }} 季</el-tag>                     <!-- 第1季也显示 -->
  <el-tag type="success" v-if="item.enable">已启用</el-tag>
  <el-tag type="info">…{{ item['subgroup'] ? item['subgroup'] : '未知字幕组' }}</el-tag>
  <el-tag type="warning">{{ item['currentEpisodeNumber'] }} / {{ item['totalEpisodeNumber'] ? item['totalEpisodeNumber'] : '*' }}</el-tag>
  <el-tag type="danger" v-if="item.ova">ova</el-tag>
  <el-tag type="danger" v-else>tv</el-tag>                    <!-- 正常态用"危险红" -->
  <el-tag v-if="item.standbyRssList.length > 0">备用RSS</el-tag>
```
- `item.releaseDate` 确实在卡片数据里（`App.vue:201` 就在用它过滤），但卡片从不渲染；列表载荷也带着 `omitCount`（漏集数）与 `notDownload`（被禁下载的集，`Ani.java:237/279`），卡片只用一句语义不明的「健康 85」概括。用户必须点进预览才知道漏没漏集，也完全看不出"周几更新"。
- 颜色语义冲突：`tv`/`ova` 都用 `danger` 红，进度却用 `warning` 橙（与"备用RSS"同色）；"第 1 季"和"未知字幕组"占了 3 列网格的两格。
- **修复**：`tv/ova` 改中性色；`season===1` 时不显示；新增「周X更新 / 上映 2024-01」「漏 N 集」（`omitCount>0` 时红色）「禁止下载 N 集」；进度改成"已下载到第 X 集"。

#### P1-5. "最近更新时间"无标签且永不刷新
**位置**：`ani-rss-ui/src/home/AniCard.vue:76-79`、`home/List.vue:92-94`、`js/format-time.js:8-13`
```js
// List.vue —— 只在加载/筛选时格式化一次
.map(it => ({...it, lastDownloadFormat: formatTime(it['lastDownloadTime'])}));
```
- 全 `src` 检索 `setInterval|useIntervalFn|useTimeoutFn` **零命中**（`useTimeoutFn` 只被 element-plus 内部使用），没有任何定时器驱动重算。挂着页面几小时后卡片仍显示"刚刚/3分钟前"，而实际可能十几个小时没有新集——**这恰恰是用户判断"RSS 还在工作吗"的唯一线索**，会造成错误的安心或恐慌；只有手动改一次筛选条件才会刷新文案。
- **修复**：30~60s 定时重算（或 `useTimeAgo`）；该行加标签"最近更新 · 3分钟前"。

#### P1-6. 卡片没有"立即检查新集"入口，单订阅刷新藏在"编辑 → 其他 → 刷新"
**位置**：`ani-rss-ui/src/home/AniCard.vue:81-99`、`home/Ani.vue:273-286, 454-459`
- 追番最常做的是"这部番现在有新集了吗"，当前路径是：点编辑图标 → 打开弹窗 → 点「其他」下拉 → 点「刷新」，4 步且都在表单弹窗里，结果只有一句 toast。用户宁可反复点顶部「刷新全部」（后者还会抢占周期任务，见 P1-41）。
- **修复**：卡片操作区加"立即刷新"按钮（tooltip），并显示该订阅的 pending 状态（任务管理器已有数据源）。

#### P1-7. 卡片操作按钮：32px 触控目标、纯图标、无 aria、删除紧贴编辑
**位置**：`ani-rss-ui/src/home/AniCard.vue:81-99`、`style.css:562-578`
- 三个纯图标按钮在卡片右下角**竖排堆叠**，`list-card-spacer` 只有 5px；Element Plus 默认按钮约 24~32px，低于 44×44 触控下限，"编辑"与"危险删除"仅隔 5px——**误删风险**。同文件的标题与字幕组是有 `el-tooltip` 的（`:13`、`:56`），此处属遗漏。
- 全项目 `aria-label` 出现 **0 次**，这三枚按钮对读屏用户是无名按钮（WCAG 4.1.2）。
- **修复**：移动端横排并撑到 44px；删除与编辑拉开距离；补 `aria-label`/`el-tooltip`。

#### P1-8. 卡片渲染缺空值防护，一次异常 = 首页空白（`score` / `decodeURIComponent`）
**位置**：`ani-rss-ui/src/home/AniCard.vue:24, 122-124`
```html
<h4 class="list-card-score" @click="emit('rate', item)">{{ item['score'].toFixed(1) }}</h4>
```
```js
let decodeURLComponentSafe = (str) => decodeURIComponent(str.replace('+', ' '));  // 非法 % 序列抛 URIError
```
- `Ani.java:213` 的 `score` 是 `Double`（可空）；URL 分支会渲染用户/源站提供的 RSS 地址（`:37-42`）。任一为 null / 含裸 `%`（如 `100%.xml`）→ 渲染期抛错 → **整块列表渲染失败，表现为首页空白且无任何提示**。另外 `replace('+',' ')` 只替换第一个 `+`，应改 `/\+/g`。

### 3.2 批量管理与删除

#### P1-9. 批量操作没有"已选 N 项"，筛选/搜索会静默清空勾选
**位置**：`ani-rss-ui/src/home/Manage.vue:222-243, 288-291, 306-335`
```js
let changeFilterList = () => {
  searchList.value = list.value.filter(filter)…      // 每次输入整体替换表格 data
}
```
- Element Plus 行为证据（已核对依赖源码）：替换 `data` 会 `clearSelection()` 并 emit `selection-change: []`（`element-plus/es/components/table/src/store/index.mjs:36`、`.../store/watcher.mjs:219-226`），而本表未设 `row-key`/`reserve-selection`（`Manage.vue:123-130`）。
- **影响**：用户勾了 10 部，想再输关键词缩小范围继续勾 —— 一敲键盘勾选全清，而界面只显示"共 N 项"（`:183`），**没有任何"已选 N 项"提示**，用户不会察觉；接着点「启用」只会收到"未选择订阅"的报错（后端 `Assert.notEmpty` 兜底，不会误伤全量，但用户的勾选工作白做）。
- **修复**：常驻"已选 N 项 / 清空选择"；筛选导致清空时明确提示；需跨筛选保留则加 `row-key="id"` + `reserve-selection`。

#### P1-10. 批量任务在后台异步执行，前端却立刻报成功并刷新出旧数据
**位置**：`ani-rss-ui/src/home/Manage.vue:320-335`、`ani-rss-application/.../AniController.java:365-390`
```js
http.updateTotalEpisodeNumber(force, ids).then(res => {
  ElMessage.success(res.message)     // "已开始更新总集数"
  reLoadList()                       // 后台还没跑完就重新拉列表
})
```
- 绿色成功提示 + 立即刷新出来的仍是**旧总集数**，用户判定"这功能坏了"，于是重复点（每点一次新增一个后台任务）；「刮削」同样只有开始提示，无 pending、无完成通知、无失败汇总。
- **修复**：按钮 pending/禁用直到轮询到结果；文案改"已加入队列，正在更新 N 条，可到任务管理器查看进度"并给跳转；失败条目汇总提示。

#### P1-11. 全部批量入口藏在一个无文字的 "…" 图标里
**位置**：`ani-rss-ui/src/home/Manage.vue:42-43`
```html
<el-dropdown :trigger="'click'"><el-button bg text icon="MoreFilled"/></el-dropdown>
```
- 启用/禁用/刮削/更新总集数/导入/导出/删除这 7 个动作只存在于这个无标签按钮里（同项目 `Ani.vue:274` 至少写了"其他"两字），且位置在筛选行右侧、与搜索框同一排，容易被当成"刷新/设置"而错过。配合 P1-9 的无选中提示，批量能力的发现成本很高。

#### P1-12. 删除确认不列出将被删除的订阅；多选 + 删文件时路径为空；且路径被当 HTML 渲染
**位置**：`ani-rss-ui/src/home/Del.vue:4-11, 57-80`
```html
<div v-else><el-text size="large">是否删除共 {{ aniList.length }} 个订阅?</el-text></div>
<el-checkbox v-model="deleteFiles" class="el-checkbox-danger">同时删除下载任务与本地已下载的文件</el-checkbox>
```
```js
let downloadPath = ''
if (aniList.value.length === 1) {                  // 只有单选才取路径
  downloadPath = (await http.downloadPath(aniList.value[0])).data['downloadPath']
}
ElMessageBox.confirm(`<strong …>将会删除整个文件夹, 是否执意继续?<br>${downloadPath}</strong>`, '警告',
    {dangerouslyUseHTMLString: true, …})
```
- **三处问题**：①勾选框说"删除已下载的文件"，二次确认才抛出"删除**整个文件夹**"（后端 `FileUtil.del(file)` 递归删目录，`AniController.java:261`），而该目录可能含用户手工放入的其他内容；②批量删除时 `downloadPath` 是空串，警告框第二行**空白**，最危险的一步信息最少；③标题来自 RSS/用户输入，`dangerouslyUseHTMLString: true` 把它当 HTML 渲染，存在注入面且 `<`、`&` 显示异常。
- **修复**：确认框列出前 N 个标题；批量时展示公共父目录与条目数；勾选框文案直接写明"删除整个下载目录（含目录内其他文件）"；不用 `dangerouslyUseHTMLString`，改组件式内容或转义。

#### P1-13. 文案残留英文与前后端矛盾
**位置**：`home/Cover.vue:104-113`、`home/Collection.vue:167-171, 304-315`、`config/basic/Other.vue:80-82` vs `ConfigController.java:154-156`
```js
ElMessage.error('Avatar picture must be JPG/PNG format!')
ElMessage.error('Avatar picture size can not exceed 1MB!')
ElMessage.error('Avatar picture must be .torrent format!')
```
```html
<el-text type="warning">切换后服务将自动重启，海外 VPS 推荐选择 IPv4 优先</el-text>
```
```java
return Result.success("网络协议设置已保存，请手动重启服务: systemctl restart ani-rss 或 ani-rss restart");
```
- 中文界面里弹英文"Avatar picture"（头像）错误，明显是 Element Plus 示例代码未清理；`Collection.vue:169` 又写".torrent 文件小于 5M"而代码校验 10MB；网络协议说明与后端**直接矛盾**，且"需手动重启"这条关键信息只是一句会消失的 toast。`Add.vue` 中同一来源还有两个名字（`label="AG"` 与按钮 "AnimeGarden"）。

### 3.3 添加订阅流程

#### P1-14. 标签页 = RSS 解析策略，界面却没有任何说明；真实失败原因被吞
**位置**：`ani-rss-ui/src/home/Add.vue:12-148, 183-192, 214`、`ani-rss-application/.../AniUtil.java:226-235`、`MikanService.java:480`
```js
const getRss = () => {
  if (activeName.value === 'other') { ... }
  ani.value.type = activeName.value      // 标签页直接决定后端解析分支
  http.rssToAni(ani.value)
}
const activeName = useLocalStorage('add-active-name', 'mikan')   // 默认 mikan
```
- 四个标签页长得几乎一样（都是"RSS 地址"文本框 + 一个浏览按钮）。把任意站的合法 RSS 粘进默认的 Mikan 页 → 报"RSS解析失败 获取失败"；而真实原因往往可操作（`MikanService.java:480`：`"Mikan 被 Cloudflare 拦截, 请配置代理"`），却在 `AniUtil.java:233-235` 被统一吞成 `"获取失败"`。
- **修复**：标签页改名"来源/解析方式"并各写一句差异；前端做域名嗅探自动选 `type` 或提供"自动识别"；错误原因透传（至少提示"可能被 Cloudflare 拦截，请检查代理"）。

#### P1-15. "已订阅"只标在番剧行，单个添加无法追加第二个字幕组
**位置**：`home/Mikan.vue:88-89, 113-115`（AniBT/AG 同构）、`home/Add.vue:228-236` vs `Mikan.vue:341-350`
```html
<el-badge v-if="it['exists']" … value="已订阅"/>            <!-- 只标番剧行 -->
<el-button bg @click.stop="callback(group)" icon="Plus">添加</el-button>   <!-- 每个字幕组都能点 -->
```
- 主流路径（点"添加"）**只能加一个字幕组**，批量添加却会写 `standbyRssList`（`Mikan.vue:341-350`）——两条路径能力不一致。用户想加第二个字幕组时，走单个添加会在最后一步撞上"订阅标题重复"或被静默替换（P1-16），界面没有任何指向"备用 RSS"的引导。
- **修复**：字幕组行增加"加为备用 RSS"；已订阅时把当前主源那行标为"当前主源"、其余给提示。

#### P1-16. 开启"自动替换"时同名订阅被静默删除，UI 却说"添加订阅成功"
**位置**：`ani-rss-application/.../AniController.java:83-94, 116`、`ani-rss-ui/src/home/Add.vue:205-211`
```java
if (replace) {
    AniUtil.getAniList().remove(first.get());   // 静默删除旧订阅，只写日志
} else {
    throw new IllegalArgumentException("订阅标题重复");
}
...
return Result.success("添加订阅成功");
```
- 旧订阅的匹配/排除规则、集数进度随之消失，界面只回一句成功。默认 `replace=false` 时则报"订阅标题重复"，同样不解释原因与出路。
- **修复**：添加前用 picker 返回的 `exists` 提示"已存在同名订阅：继续将替换 / 请改用备用 RSS"；替换生效时二次确认。

#### P1-17. AnimeGarden 页没有搜索框；AniBT 不重置关键词；Mikan 季度下拉残留
**位置**：`home/AnimeGarden.vue:40-44, 163-165`、`home/AniBT.vue:178-186`、`home/Mikan.vue:53-54, 219-229`
- AG 对话框整个没有 `el-input`，只能浏览当季 subjects（后端 `AnimeGardenService.java:34-56` 只在有 `bgmUrl` 时精确返回）；AniBT 的 `show()` 不重置 `title`（对比 `Mikan.vue:179` 有 `text.value=''`）；Mikan 搜索态下季度下拉被 `:disabled="text.length > 0"` 锁死，且 `seasons` 为空时不覆盖 → **残留上次季度**。
- **修复**：AG 补关键词搜索；`show()` 统一重置；允许"关键词 + 季度"并存并清空旧季度。

#### P1-18. AniBT 批量添加后调用不存在的 `list()`，成功后反而弹错误
**位置**：`ani-rss-ui/src/home/AniBT.vue:325-332`
```js
ElMessage.success("添加成功")
// 刷新 AniBT 列表以更新"已订阅"标记
list()                    // ← 本组件只有 search()，无 list()
window.$reLoadList()
} catch (e) {
  ElMessage.error(e)      // 接住 ReferenceError，弹不可读错误
```
- 订阅其实已全部添加成功，用户却看到红色错误；且 `window.$reLoadList()` 被跳过 → 主列表不刷新、"已订阅"标记不更新 → 用户以为失败并重复添加。**一行可修**。

#### P1-19. 批量添加：不可取消、单条失败即中断、进度提示用成功样式、"添加中"后无收尾
**位置**：`home/Mikan.vue:2-13, 310-368`（AniBT/AG 同构）
```js
ElMessage.success("添加中....")      // 进行中提示用了 success 样式
...
await http.addAni(ani)               // 任意一条失败 → 直接进 catch，剩余全部不执行
```
- 对话框强制 `:show-close="false"`、禁止 Esc 与点遮罩（`Mikan.vue:4-6`），没有取消按钮；批量 10 部时第 3 部失败，用户既不知道哪些成功也不能中止。
- **修复**：逐条 try/catch 汇总"成功 N 条，失败 M 条（原因）"；进度框加取消；进行中文案改 info/loading。

#### P1-20. 添加成功后没有去处，且可能被顶栏筛选"藏起来"
**位置**：`home/Add.vue:205-211`、`home/List.vue:108-123`、`home/App.vue:14-20`
- 只 `ElMessage.success` + `$reLoadList()`，无滚动定位/高亮；列表重载后仍套用顶栏搜索词与筛选（`List.vue:118`），新订阅完全可能不出现在屏幕里 → 用户认为"没加上"并重复操作。

#### P1-21. "Other" 必填项标错位置
**位置**：`home/Add.vue:115-133, 184-189`、`AniUtil.java:266`
- "番剧名称"写着"请勿留空"，真正被后端强校验的却是 `bgmUrl`（`Assert.notBlank(bgmUrl, "bgmUrl 不能为空")`）。用户填了标题、贴了自建站 RSS，却因没有 bgm.tv 条目而加不进去，提示也不解释原因。
- **修复**：BgmUrl 标必填 + 说明用途（刮削/评分/集数匹配），或提供"无 BGM 条目"降级路径。

#### P1-22. 添加前看不到"将下载哪些集"，预览按钮藏在第二屏
**位置**：`home/Add.vue:149-155`、`home/Ani.vue:308`
- "选字幕组 → 匹配"这步没有任何摘要，用户必须先点「确定」进表单、再点「预览」才知道会命中哪些集（多两步，而这是本可前置的信息）。
- **修复**：在匹配弹窗给出"该 RSS 最近 N 条 / 覆盖 E01-E12"摘要，或把预览提到确认之前。

### 3.4 设置与配置

#### P1-23. 保存后不区分"立即生效 / 需重启 / 需重登"
**位置**：`config/basic/Other.vue:80-82`、`ConfigController.java:154-156`（同 P1-13 后半）
- **修复**：改为常驻 `el-alert`；保存结果分级；"需重启"落到实处（提供"立即重启"按钮，复用 About 的 `http.stop(0)`）。

#### P1-24. 设置项无即时校验，失败提示不指到字段
**位置**：`config/Download.vue:12-14, 297-299`、`config/Proxy.vue:21-30`、`ConfigUtil.java:645-660`
```js
let testPathTemplate = (path) => new RegExp('\\$\{[A-z]+\}').test(path);   // 只要含任一 ${x} 就通过
```
- 下载器地址可填 `192.168.1.5:8080`（漏协议）而不报错，后端 `ConfigUtil.formatUrl()` 静默归一化；路径模板只要有任意一个 `${x}` 就通过校验（`${letter}`、`${tmdbid}` 等匹配不严谨）；代理开关打开但 Host 为空时只能拿到后端一句"代理参数不完整"。
- **修复**：URL 失焦即时校验并回显归一化结果；用与后端一致的变量白名单校验所有路径字段；保存失败时定位并高亮对应页签。

#### P1-25. 首次配置没有任何引导与自检
**位置**：`home/Config.vue:72-85`、`config/Download.vue:12-14`
- 首屏落在「下载设置」，无必填标记、无步骤指引、无"配置检查"。最小闭环（下载器地址 → 保存位置 → 通知）散落在 2 个页签多个折叠面板里；**不配下载器也能"保存成功"**，首页也没有任何"未配置完成"提示，直到订阅不下载才发现。
- **修复**：首屏"快速开始"清单（3 步 + 状态灯）；关键字段必填标记；保存后跑一次配置自检。

#### P1-26. 日志条数选项与后端 512 上限脱节且静默截断
**位置**：`config/basic/Other.vue:24-30`、`js/config.js:37`
```html
<el-option v-for="it in [128,256,512]" :key="it" :label="it" :value="it"/>
```
```js
"logsMax": 2048,     // 前端兜底值
```
- 后端 `FixedSizeLinkedList` 把 >512 静默改回 512 并写回配置，用户既不知道上限，也会遇到"下拉里显示的值不在选项里"。

#### P1-27. 前端兜底默认值含作者本机 macOS 路径
**位置**：`js/config.js:15-16, 119`
```js
"downloadPathTemplate": "/Users/wushuo/Movies/番剧/${letter}/${title}/Season ${season}",
"ovaDownloadPathTemplate": "/Users/wushuo/Movies/剧场版/${letter}/${title}",
"completedPathTemplate": "/Users/wushuo/Movies/已完结番剧/${letter}/${title}/Season ${season}",
```
- 后端默认是平台感知的（`ConfigUtil.java:60-70`）。一旦 `api/config` 失败，表单显示作者本机路径，Linux/Docker 用户照抄保存即写到不存在的目录。同类：`config.js` 缺约 20 个字段、`titleYear`/`logsMax` 与后端默认不一致（双份真相）。

#### P1-28. 「测试」入口不统一、结果不可读
**位置**：`config/Download.vue:135-140, 286-295`、`config/Proxy.vue:42-57`、`config/notification/EmbyRefreshNotification.vue:19`、`ConfigController.java:239-245`
- 下载器测试按钮藏在表单中部、无 label；Emby 拉媒体库是**无文字纯图标**按钮；`Proxy.vue` 的 label 是英文 `ScrapeTest`（唯一把 status/time 留在界面上的），其余全是转瞬即逝的 toast。后端只对 qBittorrent/OpenList 给具体原因，Transmission/Aria2 用户只能拿到"登录失败"。
- **修复**：统一"测试"+图标+**持久结果区**（成功/失败原因/耗时）；分下载器给出分项失败原因。

#### P1-29. 设置无法搜索，页签/折叠命名是英文与行话
**位置**：`config/Basic.vue:3-29`、`config/basic/Other.vue:4`、`config/Download.vue:71`
- 找"OAuth 授权"要先猜到它藏在「基本设置 → Bangumi → 获取方式=自动获取」（`Bangumi.vue:8-13`）；找"IP 白名单/反代"藏在「登录设置」；`Api`/`BgmApi`/`Driver`/`SendKey`/`apiUrl` 等英文 label 基本无可读性且无 tooltip/文档链接。

#### P1-30. 外观/主题色的「取消」撤不回来（保存语义不一致）
**位置**：`config/basic/Page.vue:24-49, 58-69`、`js/global.js:21,26,31-51`
- `store`（`useColorMode`）、`color`、`maxContentWidth`、`showScore/showWeek/showPlaylist/showLastDownloadTime` 都是**直接 `v-model` 到 localStorage ref**，赋值即写盘；`@active-change="colorChange"` 意味着**鼠标在取色盘上移动时就已生效**。而弹窗的「取消」/Esc 只是关窗（见 P0-5）。
- **影响**：用户只想"试试深色/换个主题色"，点取消后外观已被**永久改变**（localStorage 跨会话保留）；同时它们和真正的配置字段混在同一个「页面设置」表单里，用户无法区分哪些改了立即生效、哪些要点确定。
- **附带**：`saveConfig` 把这些 ref 对象一并 POST（`Config.vue:89,99`），后端 `Config.java` 无同名字段，被 Gson 静默忽略（已核对 `WebMvcConfig.java:50,80` + `GsonStatic.java:17-25`）——不影响功能，但说明该数据流本身是"双份真相"，且"导出设置"不会带走外观偏好（换设备后外观重置）。
- **修复**：外观类项移出后端保存表单，单独分组并注明"仅本机"；给「取消」加外观回滚（进入弹窗时快照 `store/color/maxContentWidth`）。

#### P1-31. 下载器/通知/密码类字段的掩码与只读策略不一致
**位置**：`config/LoginConfig.vue:4-19, 63-71`、`config/Proxy.vue:21-30`
- 登录密码与代理密码是**明文**（全项目只有 `Download.vue:22/49/125`、`MailNotification.vue:21` 用了 `show-password`）；`Api Key` 字段 `readonly` 且首次为空，但 `Other.vue:110-118` 的"复制 emby 自动点格子 api"会照抄 `api-key=` 空值，且没有一处说明 Api Key 的用途。

### 3.5 可观测性与任务

#### P1-32. 订阅级失败只有计数、没有归因，日志也无法按关键词检索
**位置**：`home/TaskManager.vue:24-31`、`home/Logs.vue:4-22`、`RssTask.java:302-313`
```html
<span v-if="status.subscriptionFailed > 0" class="progress-failed">失败 {{ status.subscriptionFailed }}</span>
```
```java
} catch (Exception e) {
    RssJobState.subscriptionFailed.incrementAndGet();
    log.error("{} {}", title, ExceptionUtils.getMessage(e));
}
```
- 任务管理器只说"失败 3"，不说是哪 3 个订阅、失败在哪一步（拉取 RSS / 解析 / 提交下载）。唯一线索是 `log.error("{} {}", title, message)` 那一行，而日志面板只支持"级别 + Java 类名"过滤（同 P0-2），**普通用户无法用番剧名检索这行日志**。
- **修复**：`RssJobStatus` 增加 `failedItems[{aniId,title,stage,humanizedMessage,suggestion,at}]`（复用现成的 `TaskFailureHumanizer`），任务管理器用可展开列表呈现 + "下一步"指引；日志面板补关键词搜索。

#### P1-33. 失败队列明细首次打开不加载，计数与明细长期不同步，两个"刷新"语义冲突
**位置**：`home/TaskManager.vue:79-87, 158-171, 535-541`
```js
const show = () => {
  dialogVisible.value = true
  pollStatus()
  if (Number(status.value.failedQueueCount || 0) > 0 || failedItems.value.length) { loadFailedQueue() }
}
```
- `show()` 判断的是**上一次会话残留的** `status`；首次打开/刷新页面后 `status` 是 `emptyStatus()`，所以即使后端有 5 条失败记录，明细也不加载，用户看到"失败队列 5 / 点击刷新加载明细"。页面上同时存在两个都叫「刷新」的按钮（底部刷新任务状态、区块内刷新失败明细）。轮询只更新 `failedQueueCount`，明细在打开期间永不自动刷新，计数增长而列表陈旧且无"数据陈旧"标识。
- **修复**：`show()` 里先 `await fetchStatus()` 再按真实计数决定是否加载；底部按钮改名"刷新状态"；区块内改"加载明细"并在计数变化时自动重载。

#### P1-34. 「重试」无条件回报成功，失败被后台线程吞掉
**位置**：`home/TaskManager.vue:771-783`、`AniController.java:618-628`
```js
ElMessage.success(res.message || '已提交精确重下')
```
```java
ThreadUtil.execute(() -> {
    try { downloadService.retryFailedItem(snapshot); }
    catch (Exception e) { log.warn("精确重下失败 {}: {}", snapshot.getReName(), ExceptionUtils.getMessage(e)); }
});
return Result.success("已提交精确重下");
```
- 接口永远返回 200 + 绿色 toast；真实结果在后台线程里，失败只写 `log.warn`。例如"RSS 中已找不到该条目（可能已过期），无法精确重下"（`DownloadService.java:761`）这条分支**既不入队也不更新条目**——用户看到的是：点重试 → 绿色成功 → 列表纹丝不动、`attempts` 不变、无任何解释。成功后旧行也要手动刷新才消失。
- **修复**：重试返回可追踪记录或前端轮询条目 `attempts/failedAt/message` 变化；失败用 `ElMessage.error` 展示 humanized 原因；成功后立即移除该行。

#### P1-35. 运行中/异常文案未人话化，只有"上一轮结果"和失败队列走了 humanizer
**位置**：`RssTask.java:350-353, 1070-1078`、`home/TaskManager.vue:20-23, 64`
```java
RssJobState.jobMessage.set("异常: " + message);      // 正在发生的异常原样渲染
```
```java
if (rawLast.contains("失败") || rawLast.contains("异常") || ...) {   // 仅 last-finished 触发
    var h = TaskFailureHumanizer.humanize(rawLast);
```
- `TaskFailureHumanizer` 已能区分"网络/鉴权/磁盘满/离线超时/坏种"并给出处置建议，但只作用于 `last-finished` 卡片与失败队列；**正在发生**的异常走 `jobMessage = "异常: " + raw`，用户看到英文/技术栈文本。触发条件还依赖文案里出现"失败/异常/超时"等中文词（`Connection reset` 不带这些词就落到 `UNKNOWN`）。
- **修复**：`running` 分支的 message 同样 `humanize()`（保留 raw 供"复制原始错误"）；触发改为"结构化进入错误分支即触发"。

#### P1-36. 日志「清空」是无确认的图标按钮，三个按钮无语义
**位置**：`home/Logs.vue:24-28, 100-109`
```html
<el-button icon="Download" bg text @click="downloadLogs"/>
<el-button icon="Refresh" bg text @click="getLogs" :loading="getLogsLoading"/>
<el-button icon="Delete" bg text @click="clear" :loading="clearLoading"/>
```
- 三个纯图标按钮相邻、无 tooltip、无文字，语义靠猜；「清空」没有二次确认，点错就丢掉现场证据（`LOG_LIST.clear()`），而这恰好是排查"为什么没下载"时最需要的东西。
- **修复**：加 `title`/tooltip 与文字；清空走 `ElMessageBox.confirm`，并提示"仅清空界面缓存，磁盘日志仍可下载"。

#### P1-37. 通知测试恒返回成功且文案是英文 "success"；运行期通知失败 UI 不可见
**位置**：`config/NotificationConfig.vue:122-135`、`NotificationController.java:52-57`、`TelegramNotification.java:40-43,111-114`
```java
baseNotification.test(notificationConfig, ani, "test", NotificationStatusEnum.DOWNLOAD_START);
return Result.success();
```
```java
public void test(...) { send(...); }        // 丢弃 send() 的 Boolean
if (StrUtil.isBlank(telegramChatId) || ...) { log.warn("telegram 通知的参数不完整"); return false; }
```
- `send()` 在参数不全时**返回 false 而不抛异常**，控制器仍 `Result.success()`（其 message 字面量是 `"success"`），前端弹一个内容为 "success" 的绿色提示——"测试成功"与"根本没发出去"在 UI 上无法区分，恰好命中最常见的"通知收不到"场景。
- 测试固定用 `DOWNLOAD_START` 与固定番剧/集数，而 `${emoji}`/`${action}` 随状态变化——只启用 ERROR 通知的用户测的是另一套渲染；测试结果也不回显渲染后的正文，用户无法确认模板变量是否被正确替换。运行期失败（含"通知队列已满，丢弃通知"）只有日志。
- **修复**：`test` 返回结构化结果（成功/渲染后文本/错误原因），前端用 `ElMessageBox` 展示"实际发送内容 + 通道 + 结果"；允许选择状态类型；通知页顶部显示"最近一次发送：时间/通道/结果"。

#### P1-38. 状态一致性：进行中的离线下载在"预览"里显示为"未下载"
**位置**：`home/Preview.vue:50-55`、`AniController.java:509-519`、`TorrentUtil.java:114-164`
```java
item.setHasDownloaded(false);
File torrent = TorrentUtil.getTorrent(ani, item);     // 只查正式记录
if (torrent.exists()) { item.setHasDownloaded(true); continue; }
```
- OpenList/115 离线提交后记录写在 `torrents/.pending/`，而 `previewAni` 只检查正式记录与文件 → **正在下载的那一集在预览里是"本地存在：否"**，同一时刻任务管理器却显示"OpenList 离线处理中 45%"。两处信息互相矛盾，用户会误判为"没在下载"而重复点「强制下载」（该按钮会先删除已有文件/记录再重新提交，`Preview.vue:26`）。
- **修复**：`hasDownloaded` 增加第三态"下载中（pending）"并透出 `state/progress`；预览表格加"状态"列；两处共享同一份状态语义。

#### P1-39. 下载列表状态是英文枚举，且 Transmission 把"暂停/错误"映射成 downloading
**位置**：`home/TorrentsInfos.vue:41-43`、`download/Transmission.java:181-202`
```html
<el-tag type="primary">{{ torrentsInfo['state'] }}</el-tag>
```
```java
TorrentsInfo.State state = TorrentsInfo.State.downloading;
if (item.get("status").getAsInt() == 6) { state = TorrentsInfo.State.stalledUP; }
if (item.get("isFinished").getAsBoolean()) { state = TorrentsInfo.State.pausedUP; }
```
- 界面直接渲染 `stalledDL` / `checkingResumeData` / `pausedUP` 这类英文枚举；更严重的是 Transmission 只映射 3 种状态，**被手动暂停（status=0）或因错误停止的种子也显示为 "downloading"**——用户看到"下载中"而实际早已停住。
- **修复**：前端做 `state → 中文标签 + 颜色` 映射（下载中/做种中/暂停/排队/校验中/停滞/错误/缺失文件），`unknown` 显示"未知（下载器未返回状态）"；后端补齐 Transmission 映射。

#### P1-40. 下载列表没有速度 / 剩余时间 / 已下载字节，也不能跳转下载器
**位置**：`home/TorrentsInfos.vue:28-45`、`download/qBittorrent.java:356-388`、`entity/TorrentsInfo.java:101-117`
- qB 响应里的 `dlspeed`/`eta`、Aria2 的 `downloadSpeed` 都没映射到实体。面板只有进度百分比 + 一个绿色大小标签（注意 `formatSize` 是 `progress()` 里用 **size** 算出的**总大小**，不是已下载量，紧挨状态 tag 极易被读成"已下载"）。用户无法判断"是在慢慢下还是已经卡死"——这是"为什么没下载下来"最需要的一个数字。面板也是纯只读的：不能暂停/删除/重新校验，也不能一键跳转下载器 Web UI。
- **修复**：补 `dlspeed/eta/numSeeds/addedOn`；展示"速度 / 剩余时间 / 已下载-总大小"；停滞（0 速度超 N 分钟）行加重警示；提供"打开下载器"外链。

#### P1-41. 首页"刷新"会抢占并中断正在跑的周期扫描，确认框与提示都没有说明
**位置**：`home/App.vue:92-104`、`RssTask.java:398-420`
```html
<popconfirm title="立即刷新全部订阅?" @confirm="refreshAni">
```
```java
RssJobState.cancelReason.set(CancelReason.PREEMPT);
RssJobState.cancelRequested.set(true);
RssJobState.jobMessage.set("手动刷新抢先中，等待周期任务退出...");
```
- 确认框只说"立即刷新全部订阅?"，没有告知这会**取消正在进行的周期扫描**（本轮剩余订阅不再处理，OpenList 场景还会清理远端离线任务）。用户以为只是"再刷一次"，实际打断了正在跑的一轮；返回的"已请求周期任务让路"又以绿色 success toast 呈现，语义偏中性、容易被忽略。"排队待执行""任务繁忙"等非成功语义同样走绿色提示。
- **修复**：确认框按当前状态动态提示（空闲→"立即刷新"；忙碌→"当前正在扫描，刷新将中断本轮扫描并抢先执行，是否继续？"）；按语义选 `warning/info/success`。

#### P1-42. 「遗留问题修复」会移动用户文件却没有二次确认，同区清理反而有
**位置**：`home/TaskManager.vue:156-201, 639-677, 715-729`、`RssJobController.java:181-200`
- 底部并排四个语义不明的按钮（强制识别已下载 / 清理离线残留 / 清理临时目录 / 遗留问题修复），其中"遗留问题修复"会**归位并清理云盘目录结构**（动用户文件），却只需单击一次执行，误触后无法撤销；而破坏性更小的清理反而弹 confirm。四个按钮本身也没有 tooltip 说明触发场景。
- **修复**：破坏性操作统一 confirm，并写清"将移动/删除什么、影响几条"；结果用对话框展示明细（修复 N / 跳过 M）而非一闪而过的 toast。

#### P1-43. 导入：非法 JSON 弹空白 toast、无结果明细、"1MB 限制"未实现、REPLACE 无确认
**位置**：`home/ImportAni.vue:32-36, 55-57, 96-107, 144-175`、`AniController.java:703`
```js
} catch (error) {
  ElMessage.error(error.message)     // error 是 reject 出来的字符串 → undefined
}
...
reject(`JSON解析失败: ${error.message}`);   // 抛的是 String，不是 Error
```
- ①`reject` 的是字符串，`error.message` 为 `undefined` → 用户看到**没有文字的空白错误 toast**，真实原因被丢弃；②选到非数组 JSON 时显示"共 undefined 条数据"，且「开始导入」仍可点；③UI 宣称 1MB 上限但没有任何体积校验；④`data.filename` 在解析前就被赋值，解析失败也不回滚（`:144-154`）；⑤导入默认 `REPLACE`（覆盖同名订阅）无二次确认，结果只回显固定的"导入成功"，**不告诉用户新增/替换/跳过各多少条**。
- **修复**：`reject(new Error(...))` 或 `ElMessage.error(String(error))`；上传前校验扩展名/大小/`Array.isArray`；导入后展示"新增 N / 替换 M / 跳过 K"；REPLACE 前确认。

#### P1-44. 日志下载把登录令牌放在 URL 查询串里
**位置**：`home/Logs.vue:131-133`
```js
let downloadLogs = () => { window.open(`api/downloadLogs?s=${authorization.value}`) }
```
- 令牌会进入浏览器历史、反代/Nginx 的 access log 与 Referer；令牌过期或 403 时用户只在新标签页看到一坨 JSON，当前页面无任何提示（`window.open` 无法感知结果），也没有 loading 反馈。前端其它请求都走 `Authorization` 头，这里是唯一例外。
- **修复**：改 `fetch` + 头 + `Blob` 下载，失败时 `ElMessage.error`。

### 3.6 编辑 / 封面 / 评分 / 备用 RSS

#### P1-45. 「清空评分」实际清不掉（调的是"读取评分"接口）
**位置**：`home/BgmRate.vue:12-15, 53-67`、`js/http.js:317`、`BgmController.java:61-65`
```js
let clearRate = () => {
  ani.value.score = 0
  rate(ani.value)          // ← 调的是 rate，不是 setRate
}
let rate = (v) => { http.rate(v).then(res => { ani.value.score = res.data }) }
```
```java
public Result<Integer> rate(@RequestBody Ani ani) {
    Integer rate = BgmUtil.rate(subjectId, null);   // 查询当前评分，忽略请求体里的 score
    return Result.success(rate);
}
```
- 用户已评 7 分，点「清空评分」→ 星星变 0 → 接口返回 7 → 星星**跳回 7**，看起来"点了没反应"；且 `v-loading` 遮罩在请求期间挡住按钮，用户无法抢在返回前点「保存评分」——**清空评分在当前实现下无法成功**。
- **修复**：`clearRate` 改调 `setRate({...ani.value, score: 0})`，或把 GET/SET 拆成 `fetchRate`/`saveRate`；清空后直接保存并提示。

#### P1-46. 编辑订阅保存失败会让「确定」永久转圈
**位置**：`home/Edit.vue:54-63`、`home/Ani.vue:309-313`
```js
const editChange = async (fun) => {
  callback.value = fun
  let req = await http.downloadPath(ani.value)   // 失败 → 异常抛出 → fun 永不调用
```
```html
<el-button icon="Check" type="primary" :loading="okLoading" @click="async ()=>{
  okLoading = true; emit('callback',()=>okLoading = false)}}">
```
- 对比 `Add.vue:211` 的 `.finally(fun)` 是正确写法。此处失败时用户只看到一条错误 toast，按钮一直转圈，弹窗既不能重试也不能判断是否已保存。

#### P1-47. "移动文件"被连续确认两次；且该弹窗可按 Esc/遮罩关闭 = 静默放弃保存
**位置**：`home/Edit.vue:2-3, 14-25, 87-99`
- 用户已在专用弹窗点了「移动」，紧接着又被问"是否执意继续移动"，而第二个弹窗不含任何新信息（连目标路径都没有）；重复确认会训练用户无脑点确定。更糟的是该弹窗 `@close="callback"`（只复位 loading），按 Esc 或点遮罩等于**静默取消保存**，而外层表单仍停留在"已编辑未保存"状态，用户以为已保存。
- **修复**：只保留一个带路径信息的确认；该弹窗设 `:close-on-click-modal="false"`、`:close-on-press-escape="false"` 并隐藏关闭按钮，只留「移动 / 不移动」两个显式出口。

#### P1-49. 封面：输入新 URL 后预览不跟随，点「确定」提示成功但封面不变
**位置**：`home/Cover.vue:5-8, 18-21, 91-102`、`AniController.java:193-194, 709-711`
```html
<img :src="`${toApiFile(ani['cover'])}&t=${time}`" :alt="ani.title" class="cover"/>
<el-input v-model:model-value="ani.image" placeholder="https://lain.bgm.tv/pic/cover/1234.jpg"/>
<el-button :disabled="!ani.image" :loading="reLoadIng" bg icon="Refresh" text @click="reLoad"/>
```
- 预览图绑定 `ani.cover`（本地文件名），输入框改的是 `ani.image`；`setAni` 只做属性拷贝，真正把 URL 下载成本地封面的是 `refreshCover`。用户以为"点确定就换封面了"，得到"修改成功"后回到首页——封面还是旧的；必须先点那个**没有任何文字说明**的刷新图标（顺序错了还会被理解成覆盖）。
- **修复**：`watch(() => ani.image)` 或 blur 时自动调 `refreshCover` 更新预览（带 loading）；`ok()` 前若 `image` 与预览不一致则提示"封面尚未应用"；刷新图标加 tooltip 与文字。

#### P1-50. 封面上传失败完全静默
**位置**：`home/Cover.vue:23-34, 104-114`
```html
<el-upload :action="`api/upload?s=${authorization}`" :before-upload="beforeAvatarUpload"
    :on-success="res => { ani['cover'] = res.data; time = new Date().getTime() }"
    :show-file-list="false" class="upload-demo" drag multiple>
```
- 没有 `:on-error`，且 `:show-file-list="false"` 关掉了唯一的失败可视通道——上传 401/413/500 时界面毫无反应，用户只会重复拖拽。校验提示是英文并错用"Avatar"（同 P1-13）。`multiple` 在只保留一张封面时意义不大。

#### P1-51. 子弹窗的「确定」不落库，关掉外层编辑弹窗即静默丢弃
**位置**：`home/StandbyRss.vue:165-169`、`home/Preview.vue:274-281`、`home/Ani.vue:309-312`
```js
// StandbyRss.vue —— 只改父对象，不发请求
let ok = () => { check(); props.ani.standbyRssList = standbyRss.value; dialogVisible.value = false }
```
```js
// Preview.vue —— 允许/禁止下载同样只改内存
let notDownload = () => { props.ani['notDownload'].push(...selectViews.value.map(it => it['episode'])) }
```
- 这里的「确定」与别处"确定=保存"的心智模型不一致：用户在"预览"里点了「禁止下载」，表格"是否下载"列立即翻成"否"（因为直接改了 props，**看起来已生效**），关闭弹窗后若不点外层「确定」就全丢——下次刷新照样下载这几集。备用 RSS 增删同理。
- **修复**：子弹窗按钮改"应用到表单"并提示"还需点击确定保存"；或直接落库；至少在父表单标注"有未保存修改"并在关闭时确认。

#### P1-52. 备用 RSS 管理：删除无确认、按钮全图标、编辑态「确定」变灰无原因、空 URL 行静默丢失
**位置**：`home/StandbyRss.vue:81-101, 105, 150-163`
- 删除一行没有确认也没有撤销，红色垃圾桶与编辑/上移/下移仅隔 4px；按钮全是图标，三个来源站按钮也只有 logo 无文字；用户点「+」新增一行（自动进入编辑态）后发现底部「确定」变灰且**不说明原因**；若新增行没填 URL 就点 ✓ 或直接点确定，这一行会**无声消失**（`check()` 里 `.filter(it => it.url !== '')`）。URL 也没有格式校验。
- **修复**：删除加 `Popconfirm`；图标按钮补 tooltip；编辑态禁用时给 title 提示"请先点击 ✓ 结束编辑"；丢弃空 URL 行时提示；提交前做 URL 基本校验。

### 3.7 播放 / 登录 / 全局

#### P1-48. 在线播放没有任何错误反馈，加载失败即黑屏
**位置**：`play/Artplayer.vue:120-146`、`play/PlayStart.vue:27-40`
```js
art = new Artplayer({ container: '.art-app', url: src, type: extName, ... })
art.on('video:canplay', () => { ... })
```
- 播放器没有绑定 `error` / `video:error` 事件，容器固定 `700px × 450px`（`.art-app`，`Artplayer.vue:161-167`），没有 `poster`、没有加载指示、没有"格式不支持/文件不存在"的兜底；后端 `getSubtitles` 失败也不提示（`PlayStart.vue:29-39` 只有 `finally`）。
- **修复**：绑定 `art.on('video:error')` 与 `art.on('error')` 给出可行动提示（"该文件编码浏览器不支持，请用 PotPlayer/VLC 打开"），并提供"用外部播放器打开"的直达按钮。

#### P1-53. 播放入口三处断层：必须先解析完整部字幕才能开播 / 字幕默认选中竞态 / 无进度记忆
**位置**：`play/PlayStart.vue:3-6, 27-40`、`play/Artplayer.vue:104-119, 141-145`、`play/PlayList.vue:7-30`

**断层一 —— 开播前必须等服务端解析完整部 MKV 的内封字幕**
```html
<div class="flex-center content" v-loading="loading">
  <Artplayer :playItem="playItem" v-if="dialogVisible && !loading"/>   <!-- 加载期间播放器根本不存在 -->
```
```js
http.getSubtitles(playItem.value.filename).then(res => { ... }).finally(() => { loading.value = false })
```
- 内封字幕解析是**同步等待整部文件**的服务端操作，期间没有任何进度、没有跳过入口、失败也不提示；加载完成瞬间容器从 200px 跳到 700×450，是一次明显跳变。用户在"点了播放却一直转圈"时无从判断是慢还是坏了。

**断层二 —— 字幕默认选中竞态**
```js
art.on('video:canplay', () => {
  if (defaultName) { art.plugins['multipleSubtitles'].tracks([defaultName]) }
})
```
- 该插件是**异步** fetch 完全部字幕后才注册自己；`canplay` 早于注册时 `art.plugins['multipleSubtitles']` 是 undefined → 抛 `TypeError`（被 Artplayer 内部吞掉，表现为"默认字幕没生效"）；更糟的是插件在未调用 `tracks()` 时会把**所有轨道叠加显示**。判断顺序缺一道就绪检查。
- 另外 `Artplayer.vue:104-119` 的 selector 没有"关闭字幕"项，`subtitles[0]['default']=true` 也不是该插件的字段；**无字幕时菜单为空且无任何提示**，用户不知道是没字幕还是没加载出来。

**断层三 —— 选集弹窗不定位、不标注当前集，也没有进度记忆**
- 后端 `playList` 按集数升序返回，前端没有用 `episode` / `currentEpisodeNumber` 做 `scrollIntoView` 或高亮，用户每次都从第一集开始手动往下滚（`PlayList.vue:7-30`，固定 2 列 500px 区域）。
- `Artplayer` 未开启进度记忆（该 dist 的 `autoPlayback` 默认 false，且 zh-cn i18n 里**已内置**"上次看到/跳转播放"文案），也没有"已看/未看"标记。

**顺带发现的两处实现错误**
- `Artplayer.vue:133` 的 `preload: true` **不是 Artplayer 的选项**（正确位置是 `moreVideoAttr.preload`），写法无效。
- `Artplayer.vue:121` 用全局选择器 `container: '.art-app'` 定位容器，同一页面若存在多个实例会互相抢。

**修复建议（一个迭代主题：把"点开即看"做实）**
① `getSubtitles` 改为可选/可跳过（超时 N 秒先放主视频，字幕后到再挂载），并给加载进度；② `tracks()` 调用前判插件就绪（或改在插件 ready 回调里设默认字幕）；③ 字幕 selector 增加"关闭字幕"项 + 无字幕时给文字提示；④ 弹窗打开时把当前集 `scrollIntoView` 并高亮；⑤ 用 localStorage 记录"看到第几集 + currentTime"，列表标"看到这里"；⑥ 补 `art.on('video:error')` 的中文说明与"用外部播放器打开"直达（与 P1-58 合并做）。

#### P1-54. 播放列表不能续播，也不标记已看
**位置**：`play/PlayList.vue:63-77`、`play/Artplayer.vue:120-140`
- 选集弹窗每次重新拉取，没有"上次看到第几集/播放进度"记录，卡片上也没有"已看/未看"标记。追番用户每周都要自己找"我看到哪了"。
- **修复**：前端用 localStorage 记录每部番的 `filename + currentTime`，列表高亮"看到这里"，`art.on('video:timeupdate')` 节流保存并支持续播（后端无需改动）。

#### P1-55. 播放器底部菜单结构有误，三个外部播放器不可达
**位置**：`play/Artplayer.vue:5-78`
```html
<el-dropdown>
  <el-button bg text icon="MoreFilled"/>
  <template #dropdown>
    <el-dropdown-menu>
      <el-dropdown-item …>Pot</el-dropdown-item>   <!-- 5 个：Pot/VLC/IINA/MPV/Infuse -->
    </el-dropdown-menu>
    <el-dropdown-item @click="openUrl(`ddplay:…`)">弹弹Play</el-dropdown-item>
    <el-dropdown-item @click="openUrl(`anix://…`)">AnimacX</el-dropdown-item>
    <el-dropdown-item @click="openUrl(`SenPlayer://…`)">SenPlayer</el-dropdown-item>
  </template>     <!-- ← 后 3 项落在 el-dropdown-menu 之外 -->
</el-dropdown>
```
- **根因已回源确证（静态层面确定）**：`el-dropdown` 的默认插槽（`element-plus/es/components/dropdown/src/dropdown2.mjs:65-76`）把 `$slots.default` 交给 `el-only-child`；`el-only-child` 用 `findFirstLegitChild` 只取**第一个**合法子节点并 `cloneVNode` 渲染（`.../components/slot/src/only-child.mjs`）。这里的第一个子节点是 `<el-button icon="MoreFilled">`，`<el-dropdown-menu>` 及其内 5 项被整体丢弃；而 `el-dropdown-item` 的挂载依赖 dropdown 的 `provide`，落在 `el-dropdown-menu` 之外的 3 项同样无出口。
- **结论**：弹弹Play / AnimacX / SenPlayer 这 3 个外部播放器在 UI 中**不可达**；Pot/VLC/IINA/MPV/Infuse 5 项是否可用取决于 `el-dropdown-menu` 被克隆后能否正常挂载（`dropdown-menu` 的 `inject(DROPDOWN_INJECTION_KEY)` 是 Symbol、无默认值，克隆渲染会走到 `inject` 的 undefined 分支）——**建议实测一次**：打开播放器右下角菜单，确认到底能看到几个播放器。
- **修复**：把 3 个 `el-dropdown-item` 移入 `el-dropdown-menu`；补一条 DOM 级回归断言（打开菜单应能看到 10 个播放器选项）。

#### P1-56. 登录安全与引导
**位置**：`Login.vue:100-124`、`Login.vue:85-91`、`js/global.js:7-11`
- **IP 白名单静默自动登录**：命中白名单时直接 `authorization.value = new Date().getTime() + ''`（`Login.vue:103-115`），用户看到表单一闪即进首页，没有任何"已通过 IP 白名单免登录"的说明。
- **记住密码是明文 localStorage**：`rememberThePassword` 用 `useLocalStorage` 保存明文账号密码，共享设备上等于把密码留在浏览器里。
- **修复**：白名单命中时提示一次并说明如何关闭；"记住密码"改为只记用户名或提示风险。

#### P1-57. 布局低效：`isNotMobile` 简单阈值驱动，进而在窄屏出现不可用/溢出的分支
**位置**：`js/global.js:131-152`、`home/List.vue:125-131`、`home/App.vue:51-137`、`play/PlayList.vue:98-104`
```js
const initLayout = () => {
    let app = document.querySelector('#app');
    maxContentWidth.value = Math.max(maxContentWidth.value, 1200);   // 静默把用户设置抬到 1200
    app.style.maxWidth = `${maxContentWidth.value}px`
    isNotMobile.value = app.offsetWidth > 800;                      // 单一阈值
```
- 主列表按 `Math.floor(app.offsetWidth / 400)` 自适应列数，而选集弹窗固定 2 列 500px（`PlayList.vue:98-104`）；`isNotMobile` 只用于"显示/隐藏文字"，一旦为 false 顶栏就退化为纯图标（见 P1-60），没有中间态。
- **修复**：`maxContentWidth` 抬升改为显式提示或尊重用户值；选集列表与主列表共用同一套自适应规则。

#### P1-58. 首屏体积与渲染：全量图标 + 全量 EP CSS + 播放器被同步预载
**位置**：`main.js:1-11`、`ani-rss-ui/dist/index.html:12-22`（构建产物实测）、`home/List.vue` → `play/PlayList.vue` → `play/PlayStart.vue` → `play/Artplayer.vue` 全静态 import

| 产物 | 原始 | gzip |
| --- | --- | --- |
| `main.js` | 1007.7 KB | 338.6 KB |
| `element-plus.js` | 522.5 KB | 163.9 KB |
| `element-plus.css` | 351.1 KB | 47.1 KB |
| `element-icon.js` | 164.2 KB | — |
| `artplayer.js` | 151.6 KB | — |

- `main.js` 里 `for (const [key, component] of Object.entries(ElementPlusIconsVue)) app.component(key, component)` **注册了全部图标**，同时 `import 'element-plus/dist/index.css'` 引入全量样式；artplayer 被 `modulepreload` 预载，而它只在用户点开某个订阅的选集时才用得到。
- **影响**：移动网络首屏要下 ~1.4MB（gzip 后仍 ~550KB），配合 `List.vue:7` 只有 `v-loading` 没有骨架屏（`List.vue:70`），首屏是一段空白等待。
- **修复**：`defineAsyncComponent` 懒加载播放链路（`PlayList`/`PlayStart`/`Artplayer`）；图标按需注册（项目已在用 `unplugin-vue-components`，可复用其 icon resolver）；EP 改按需样式；列表加骨架屏。

#### P1-59. 登录页与播放页的可用性细节

**位置**：`Login.vue:10-35, 72-97, 103-118`、`play/PlayList.vue:21-27`、`play/Artplayer.vue:5-6, 104-119`

- **回车提交无并发守卫**：`@keyup.enter="login"` 与按钮 `@click="login"` 共用，而 `loading` 只由 `el-button` 的 loading 状态挡住点击（EP 的 loading 只作用于 click），按住回车会连发请求，**加速触发那 30 次/天的限流**（见 P0-6）。
- **登录输入框无 label**：只有 placeholder（`Login.vue:13-24`），配合 `--el-text-color-placeholder:#a8a8ad` 在 `#f5f5f7` 底上对比度约 **2.2:1**（深色下 `#6e6e73` 在 `#141416` 上约 2.7:1），不满足 WCAG 1.4.3 / 3.3.2。
- **`show-password` 眼睛图标不可键盘操作、无名称**：EP 渲染为裸 `<i onClick>`，WCAG 2.1.1 / 4.1.2 不达标。
- **纯图标按钮无名称**：`PlayList.vue:21-27` 每集一个无名播放键；`Artplayer.vue:5-6` 的 `MoreFilled` 无名——而它是"格式不支持"时唯一的出路（见 P1-48）。
- **字幕菜单缺"关闭字幕"项**：`Artplayer.vue:104-119` 的 selector 只列已有字幕，`subtitles[0]['default']=true` 并非该插件字段；无字幕时菜单为空且无任何提示。
- **首次访问零引导**：随机初始密码只打印在启动日志里（`ConfigUtil.java:297-310`），登录页没有任何提示；`test()` 用裸 `fetch` 绕开了统一错误处理，白名单探测失败时静默（`Login.vue:103-118`）。
- **触控目标偏小**：登录按钮与复选框约 32px（`style.css:611-616` + EP `--el-component-size:32px`），选集播放键 40px（满足 WCAG 2.5.8 的 24px 底线，属可用性建议）。
- **`Disable.vue:2-5` 用 `opacity + pointer-events` 伪禁用**：键盘仍可 Tab 进被"禁用"的 `el-input`/`el-button` 并回车改值，且无 `aria-disabled`；0.4 不透明度还把 `#1d1d1f` 压到约 2.5:1（`Ani.vue:240-255` 在用）。
- **首屏闪登录页**：`Main.vue:6-7` 用 `v-if="!authorization"` 直接分流，而白名单探测是异步的（`Login.vue:107-114`），没有 `initialized` 门控，已授权用户会先看到登录表单闪一下。

#### P1-60. 禁止缩放与无障碍基线
**位置**：`ani-rss-ui/index.html:8`、全项目
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
```
- `user-scalable=0` 禁止双指缩放，违反 WCAG 1.4.4（Resize text）；全项目 `aria-label` 出现 **0 次**，纯图标按钮（首页顶栏 7 个、卡片 3 个、日志 4 个、任务管理器若干）对读屏用户均无名称（WCAG 4.1.2）；只有 `tokens.css:165-175` 处理了 `prefers-reduced-motion`，没有 `prefers-contrast`。
- **修复**：移除 `maximum-scale`/`user-scalable`；为所有图标按钮补 `aria-label`；顶栏按钮加 `title`。

#### P1-61. 封面/图片无懒加载、无失败占位
**位置**：`home/AniCard.vue:5-8`、`home/Cover.vue:7`
```html
<img :src="toApiFile(item['cover'])" :alt="item.title" class="list-card-image" @click="emit('cover', item)"/>
```
- 无 `loading="lazy"`、无 `onerror` 兜底；封面来自本地缓存文件，文件缺失（清理缓存、迁移目录）时是一张破图，用户无法区分"没有封面"与"加载失败"。（列表虚拟滚动的缺失见 P1-3。）

#### P1-62. 多个弹窗硬编码固定像素宽度，窄屏必然横向溢出
**位置**：`home/TaskManager.vue:2`、`home/SwitchMaster.vue:2`、`home/Ani.vue:317`、`home/TmdbGroup.vue:2`、`config/basic/Page.vue:2,14`
```html
<el-dialog v-model="dialogVisible" center title="任务管理器" width="820px">
<el-dialog v-model="dialogVisible" center title="切换主RSS" width="520px">
<el-dialog v-model="searchThemoviedbVisible" title="选择 TMDB" width="720px" append-to-body>
<el-dialog v-model="jsDialogVisible" align-center center title="自定义JS" width="800">
```
- `style.css:129-140` 的移动端适配只改 `--el-dialog-width` 变量；而 Element Plus 的 `width` prop 会被写成内联 `--el-dialog-width`，**优先级高于媒体查询**。因此在 375px 宽的手机上，任务管理器（820px）、切换主 RSS（520px）、TMDB 选择（720px）、自定义 JS/CSS（800px）会横向溢出屏幕，内容与按钮被裁在视口外。
- **修复**：这些弹窗改用 `width="min(820px, 100%)"` 或 `class="el-dialog-auto-width"` 风格；移动端统一 `width: 100%`。

#### P1-63. 移动端缺安全区与视口单位处理；播放区高度只按 vw 计算
**位置**：全项目（`grep overscroll|100dvh|safe-area|env\(` **零命中**）、`style.css:129-140, 693-703`、`play/Artplayer.vue:161-167`
```css
.art-app { width: 700px; height: 450px;
           max-width: calc(100vw - 48px);
           max-height: calc(56.25vw - 27px); }   /* 只按 vw，横屏/矮视口会裁切 */
```
- 没有 `env(safe-area-inset-*)`，刘海屏/带底部手势条设备上顶栏与弹窗底部按钮会被遮挡；没有 `100dvh`，移动端浏览器地址栏收缩时 `height:100%` 的布局会跳动；没有 `overscroll-behavior`，弹窗 `lockScroll` 时 iOS 上仍可能出现背景滚动穿透。
- 播放区高度只用 `vw` 推导，横屏手机（如 812×375）与矮视口下会被裁切；同时 `style.css:129-134` 的 `margin-top:55px` 与 `PlayList.vue:5` 的固定 `500px` 叠加，会出现"双弹窗双滚动"。
- **修复**：`#header` 与弹窗 footer 补 safe-area padding；容器高度改 `100dvh`（带回退）；`body` 加 `overscroll-behavior: none`；播放区高度补 `vh`/`dvh` 约束。

---

## 4. P2：打磨项

| # | 问题 | 位置 |
| --- | --- | --- |
| P2-1 | 明暗模式切换藏在「设置 → 基本设置 → 页面」三级，无快捷入口 | `config/basic/Page.vue:24-42` |
| P2-2 | 全站无键盘快捷键（无 `/` 或 `⌘K` 聚焦搜索） | `home/App.vue:12-21` |
| P2-3 | 日志面板用 shiki 高亮全量拼接文本，日志多时每次过滤都全量重渲染 | `home/Logs.vue:72-86` |
| P2-4 | `v-loading.fullscreen.lock` 用于"更新/重启"（最长 2 分钟无进度、无阶段文案） | `config/About.vue:25, 156-182` |
| P2-5 | 关闭/重启确认文案不说明后果（下载中断、需手动拉起），Windows 端必然失败却先弹 success | `config/About.vue:34-44, 141-154`、`AboutController.java:39-42` |
| P2-6 | 更新/重启后 5 秒强制 `location.reload()` + 清 token，用户被强制退出登录 | `config/About.vue:141-153` |
| P2-7 | 全局排除/优先保留「清空全部」是纯图标按钮、无确认、不可撤销、不支持多行粘贴 | `config/Exclude.vue:44-53`、`config/PrioKeys.vue:29-31` |
| P2-8 | 导出/导入订阅目标文件名固定 `ani.v2.json`，无日期、无提示；`changeFile` 无 `.catch` | `home/Manage.vue:287-304`、`config/basic/Backup.vue:37-51` |
| P2-9 | 导出用 `Upload` 图标、导入用 `Download` 图标，与"上传=导入"直觉相反 | `config/basic/Backup.vue:4-5` |
| P2-10 | 备份设置被拆到「基本设置-备份」与「其他-自动备份配置」两个不互指的页签，备份路径/保留策略无说明 | `config/Basic.vue:27-29`、`config/basic/Other.vue:58-68`、`ConfigUtil.java:475-509` |
| P2-11 | 「清理缓存」无二次确认且未说明范围（实为清封面 + `configDir/img`） | `config/basic/Other.vue:46-57`、`ConfigController.java:163-175` |
| P2-12 | IP 白名单（跳过登录的安全配置）无校验、无自测入口（`testIpWhitelist` 已存在，仅登录页在用） | `config/LoginConfig.vue:39-54`、`js/http.js:397` |
| P2-13 | `Popconfirm` 确认后立即收起、无 pending/禁用态，调用方可被重复触发（"刷新全部"没有 loading） | `other/Popconfirm.vue:2`、`home/App.vue:93-104` |
| P2-14 | `formatTime` 缺"昨天"档位；未来时间戳（时钟偏差/异常 pubDate）显示"刚刚"，掩盖真实问题 | `js/format-time.js:8-36` |
| P2-15 | 合集预览把"关闭提示 ×"当作"应用字幕组"按钮，且字幕组取样只取第一条 | `home/CollectionPreview.vue:17-27, 62-83` |
| P2-16 | BGM 搜索无空态、Enter 无空值保护；结果表把 id/url 当主信息展示 | `home/Bgm.vue:7,10,13-34,50-59` |
| P2-17 | `TmdbGroup` 无空态/无错误处理，选中即关窗无反馈 | `home/TmdbGroup.vue:48-63` |
| P2-18 | 编辑页点 Mikan 图标遇非法 URL 抛 `TypeError`，"点了没反应" | `home/Ani.vue:508-521`、`home/StandbyRss.vue:204-217` |
| P2-19 | 管理表格缺"最近更新/漏集"列，URL 占 300px，无行内启用开关 | `home/Manage.vue:123-181` |
| P2-20 | `v-model:data` 用在 `el-table` 上（组件不 emit `update:data`），误导后续维护（3 处） | `home/Manage.vue:126`、`home/Bgm.vue:13`、`home/StandbyRss.vue:48` |
| P2-21 | 任务管理器两段 100+ 字技术说明常驻占位，未折叠 | `home/TaskManager.vue:142-156` |
| P2-22 | 失败队列 200 条全量渲染无过滤/分页；错误码 tag 是英文枚举；残留预览"最多 30 条"未说明 | `home/TaskManager.vue:88-122, 731-740, 896-904` |
| P2-23 | 下载列表 3 秒全量替换并重排，顺序持续抖动；`show()` 无 `pollToken` 守卫可能双轮询；进度排序的升降序语义反直觉 | `home/TorrentsInfos.vue:63-78, 100-122` |
| P2-24 | 卡片"健康分"原因仅挂原生 `title`，触屏不可见；`procrastinating`（摸鱼中）无标识却仍显示绿色「已启用」 | `home/AniCard.vue:27-35, 49-54` |
| P2-25 | 关闭"按星期展示"后周几信息彻底消失（卡片未渲染 `releaseDate`） | `home/List.vue:28-41` |
| P2-26 | 编辑弹窗关闭不提示未保存（与设置弹窗同源） | `home/Edit.vue:28-30` |
| P2-27 | 批量添加把 `Error` 对象直接交给 `ElMessage`（渲染成空/`[object Object]`） | `home/Mikan.vue:364-366`、`AniBT.vue:330-332`、`AnimeGarden.vue:294-296` |
| P2-28 | 6 处 `document.execCommand('copy')` 无失败检测却提示"已复制"（iOS Safari / 非安全上下文会静默失败） | `Mikan.vue:372-378`、`Preview.vue:209-215`、`AniBT.vue:337-345`、`AnimeGarden.vue:301-309`、`LoginConfig.vue:93-101`、`Other.vue:120-128` |
| P2-29 | 403 时静默清 token + 1 秒后自动刷新回登录页，刚填的表单全丢、无说明 | `js/api.js:41-51` |
| P2-30 | 占位/文案错别字与中英混排（"设置为时 0 不做限制"、`Select Level`、`ETA`/`Hash`、`ScrapeTest`） | `config/Download.vue:196-213`、`notification/BarkNotification.vue:17`、`home/TaskManager.vue:67-73` |
| P2-31 | 移动端弹窗布局用 `width:100% + margin-top:55px + margin-bottom:auto` 的固定常量，且弹窗内滚动区是固定 `height:500px`（`Config.vue:6-10`、`PlayList.vue:5`、`Manage.vue:127`）——矮屏/横屏下内容会顶出视口，没有 `100dvh`/`max-height` 约束，键盘弹起时输入框可能被遮 | `style.css:129-140`、`home/Config.vue:6-10` |
| P2-32 | 订阅卡标题固定 `width: 200px`、标签网格固定 `180px`，窄屏信息被压 | `home/AniCard.vue:174-201` |
| P2-33 | BGM OAuth 回调页固定 `min-width: 480px`，窄屏溢出；`window.close()` 在非脚本打开时无效也无提示 | `bgm-oauth-callback/App.vue:80-82, 141-143` |
| P2-34 | `Login.vue:7` / `About.vue:4` 用 `../public/icon.svg`、`../../public/icon.svg` 越界引用 public 资源，构建后靠脆弱的相对路径巧合工作 | `Login.vue:7`、`config/About.vue:4` |
| P2-35 | `About` 版本号 `cursor-pointer` 却无点击行为，buildInfo 仅 hover 可见（移动端不可见） | `config/About.vue:7-14` |
| P2-36 | 设置页签命名依赖默认索引（4 个页签无 `name`），插入页签会导致默认激活项漂移，也难做深链/搜索跳转 | `home/Config.vue:4-40` |
| P2-37 | WebUI 上传（`Page.vue:87-96`）的 401/403 提示来自 XHR 原始响应文本，可读性差（**注：`Authorization` 头传值经复核是正确的**，见 §7） | `config/basic/Page.vue:87-96, 134-141` |
| P2-38 | `manifest.json` 无 `maskable` 图标、无深色 `theme_color`；暗色下 `themeColorMeta` 写死 `#000000` 与 `--el-bg-color-page:#141416` 不一致 | `public/manifest.json`、`js/global.js:114-122` |
| P2-39 | 添加成功后的 toast 无"查看"动作（可点击直达新订阅） | `home/Add.vue:208` |
| P2-40 | `config.js` 兜底默认值缺约 20 个字段、`titleYear`/`logsMax` 与后端默认不一致（双份真相） | `js/config.js` vs `ConfigUtil.java:120-252` |
| P2-41 | 深色首屏白闪：`index.html` 的 head 里没有内联主题脚本，`theme-color` 写死纯黑/纯白与 `#141416`/`#f5f5f7` 不一致 | `index.html:9`、`js/global.js:114-122` |
| P2-42 | `index.html` 把 `<style>`/`<link>`/`<script>` 放在 `</body></html>` **之后**（HTML 非法，样式后置易 FOUC），且 `custom.css/js` 是绝对路径 `/api/...`（子路径部署失效） | `index.html:19-33` |
| P2-43 | 状态色对比度不达标：浅色下 tag `success≈2.0:1`、`warning≈1.9:1`、`danger≈2.9:1`，`style.css:606-609` 的 `info≈2.5:1`，`PlayList.vue:39` 的 danger 文本≈3.4:1（WCAG 1.4.3） | `css/tokens.css:33-43`、`style.css:606-609` |
| P2-44 | OAuth 回调页仅用颜色区分 tag 含义（无文字等级），且只有 `window.close()` 没有返回入口、无 catch 会留下空 alert | `bgm-oauth-callback/App.vue:35-40, 60-82, 91-107` |
| P2-45 | 关闭播放弹窗时先塌陷后播动画（`v-if` 与 transition 顺序） | `play/PlayStart.vue:2-6, 47-50` |
| P2-46 | 选集空态把"接口失败"误写成"未下载集数或 docker 映射存在问题"，且无 `catch` | `play/PlayList.vue:38-42, 63-77` |
| P2-47 | `el-scrollbar` 不可聚焦 → 键盘用户无法滚动选集列表 | `play/PlayList.vue:5` |
| P2-48 | `style.css:28` 用 `transition: all` + 全宽 `backdrop-filter` 无 `@supports` 回退（低端机/旧 WebView 掉帧） | `style.css:25-35, 693-731` |
| P2-49 | 文案/术语不统一：用户可见文案里出现 `docker`、`github`（小写）、`"code 为空"`；时间格式在 `format-time.js:8-36` 内混用相对/绝对 | `play/PlayList.vue:40`、`bgm-oauth-callback/App.vue:114`、`js/format-time.js` |
| P2-50 | `global.js:135` 静默把用户设的 `maxContentWidth` 抬到 1200（用户改成 1000 也会被改回） | `js/global.js:135` |
| P2-51 | `Bangumi.vue:130-131` 授权成功后立即 `location.reload()`，丢失当前设置页签与上下文，也无 `postMessage` 回传 | `config/basic/Bangumi.vue:130-131` |
| P2-52 | 主列表只有 `v-loading` 无骨架屏，配合首屏 1.4MB（见 P1-58）等待期是纯空白 | `home/List.vue:7, 70` |
| P2-53 | `AniCard` 封面图可点击但不可键盘操作（`<img @click>` 无 tabindex/按钮语义） | `home/AniCard.vue:5-8` |

---

## 5. 结构性建议（中长期）

1. **统一反馈组件**：为"加载中 / 空 / 失败可重试"三态做一个 `<AsyncSection>` 包装组件，替换 22 处裸 `v-loading`。这是最高杠杆的结构性改动——一次性消掉 P0-1、P0-3 以及同源的十几处"空白或误导界面"。
2. **统一保存语义**：设置/编辑类弹窗一律采用"脏数据追踪 + 就地保存或关闭前确认"，禁止出现"同一屏两个语义不同的确定"（当前至少 4 处：设置弹窗、通知弹窗、编辑弹窗内的预览/备用 RSS 子弹窗、外观设置的即时生效项）。
3. **系统自检页（Doctor）**：一行一件事的自检清单——Mikan/AniBT/AG 可达性、BgmApi/Token 有效性、下载器登录、保存路径可写、OpenList 连通、通知发送、磁盘剩余。用户最痛的不是"功能少"，而是"我配了但不知道哪一步断了"。现有 `downloadLoginTest` / `testNotification` / `testProxy` / `testIpWhitelist` 已具备全部素材，只差一个汇总页与"下一步该做什么"的指引。
4. **统一可访问性基线**：所有图标按钮补 `aria-label`；移除缩放限制；触控目标 ≥44px；状态不只靠颜色区分（健康分加文字等级、`tv/ova` 改中性色）。可在 `style.css` 层做一次全局收口。
5. **后端补齐展示字段**：`Log.timestamp`、`RssJobStatus.failedItems[]`、`TorrentsInfo.dlspeed/eta/numSeeds`、`hasDownloaded` 的 pending 三态——都是"用户想知道但前端拿不到"的信息，后端大多已经有了，只差透出。
6. **日志链路升级**：支持按文件读取最近 N 行（重启后也能看）、结构化 `timestamp`、服务端按关键词过滤，替代当前"全量下发 + 前端拼接高亮"。

---

## 6. 附：审计方法与覆盖

- **分片**：①首页列表/卡片/批量管理 ②设置与配置 ③任务与日志可观测性 ④添加订阅主流程 ⑤播放/登录/移动端/可访问性。
- **交叉验证**（父审计员回源核对通过的断言）：Element Plus 弹窗默认 `closeOnClickModal/closeOnPressEscape=true`、`el-dropdown` 默认插槽的单子节点行为与依赖实现、`Log` 实体字段、`addAni` 的静默替换分支、`clearCache` 的实际删除范围、`WeekComparator` 的排序起点（今天在首位）、`importAni` 的合并语义、`el-table` 替换 data 触发 `clearSelection`、`AuthUtil` 的登录限流参数（30 次/1 天）、`v-loading`(22)/`el-empty`(4)/`aria-label`(0)/`el-tooltip`(14) 的全量计数。
- **未验证项**：涉及运行时渲染与真机表现的结论（P1-55 播放器菜单/选项可见性、P1-53 的字幕竞态与开播等待、P1-59 的触控与对比度、P2-31 的移动端弹窗高度、P2-45/48 的低端机动效）建议在浏览器或真机实测一次；本次为纯静态审计，未启动服务、未修改任何业务文件。
- **构建产物的使用口径**：P1-58 的体积数字取自仓库内 `ani-rss-ui/dist/`（构建时间 2026-09-09，晚于相关源码，可用于体积结论）。但注意该 dist 的分包与源码**不是一对一**：`el-dropdown-menu`、`任务管理器` 等标识并未出现在 `main-*.js` 里（`dropdownKls` 落在 `element-plus-*.js` 分包），因此**不要**用"字符串在 main 包里找不到"来推断源码里没有对应结构；运行时结论仍以源码 + 依赖实现为准。

---

## 7. 经复核**不成立**的候选问题（避免误修）

| 候选 | 复核结论 |
| --- | --- |
| `Manage.vue` 批量操作在未选中时会对全库生效 | **不成立**。后端 `batchEnable`/`updateTotalEpisodeNumber`/`batchScrape` 均有 `Assert.notEmpty(ids, "未选择订阅")` 兜底（`AniController.java:397/366`、`ScrapeController.java:44`），实际只会收到报错。真实问题降级为 P1-9（勾选被静默清空 + 无"已选 N 项"提示）。 |
| `Page.vue` 的 `:headers="{Authorization: authorization}"` 会写成 `[object Object]` 导致 WebUI 上传失败 | **不成立**。`@vue/compiler-sfc` 对 setup 中的 ref 绑定生成 `_unref(authorization)`；`el-upload` 侧再 `String(value)`。请求头是正确的 token。真实问题降级为 P2-37（401/403 提示文案不可读）。 |
| `Del.vue` 的 `el-checkbox-danger` 红色复选框样式缺失 | **不成立**，`style.css:213` 有定义。 |
| `AniCard` 的 `.el-button .el-icon{width:15px;height:15px}` 会压扁图标 | **不成立**，15px 与 Element Plus 图标默认尺寸一致。 |
| 全局模板 `${notification}` 无限递归 | **不成立**（属稳定性专项，已修复）。 |
| `el-avatar`（`BangumiMe.vue:16`、OAuth 回调页）缺少 `alt` 违反 WCAG 1.1.1 | **不成立**，Element Plus 的 `el-avatar` 会渲染 `alt=""`。 |
| 构建产物重复加载 Element Plus CSS | **不成立**，`dist/index.html` 里 EP 样式只出现一次。 |
| `el-button :loading` 已能防住登录重复提交 | **不成立**。EP 的 loading 只拦截 **click**（`use-button`），`Login.vue:10-11` 的 `@keyup.enter="login"` 不受约束——按住回车仍会连发请求。故该条保留为 P1-59。 |
| 关闭播放弹窗会丢失选集列表的滚动位置 | **不成立**，`PlayList` 弹窗未被卸载，滚动位置能保持。 |
| `ani-rss-ui/dist/` 是过期构建、不能用于验证 | **不成立**（父审计员先前误判，已更正）。dist 构建时间晚于相关源码，体积结论有效；`el-dropdown-menu` 等标识只是落在 `element-plus-*.js` 分包而非 `main-*.js`。 |
